# 版本发布说明 · v1.9.1

> **Fortune-PMS 项目管理系统** · 发布日期 2026-09-23
> 版本跨度：v1.8.5 ~ v1.9.1（上一发布版本 v1.8.4）
> 质量基线：`tsc` 0 错误 · ESLint **0 错误 0 警告** · 单元/组件测试 **139/139** · e2e 集成测试 **13/13**（累计 152/152）

---

## 🌟 版本亮点

1. **项目类型体系成型（v1.8.5 ~ v1.9.0 六连击）**：新建项目必选类型 → 模板分类归一化 → 五类扩展 → 咨询/实施系统模板 → 六类定稿（新增「运维增强」）
2. **结项一键合并**：「运维增强」项目可将台账整体合并到目标运维项目，转绑可核对、重复合并有守卫
3. **台账权限口径统一**：修复「看得到列表、打不开详情」割裂（owner/member/guest 补授 `oplog.view`，存量库自动回填）
4. **技术债全量清零**：ESLint 警告 35 → 0（含 18 项 React Hooks 依赖修复），recharts 拆独立 chunk 提速非图表页面
5. **e2e 集成测试沉淀进 CI**：真实服务 + 临时库全链路 13 项断言，`check → lint → test → build` 四道闸门自动把关

---

## ✨ 新增功能

### 项目类型体系（v1.8.5 ~ v1.9.0）

- **必选项目类型**：新建项目强制从六类白名单选择（运维/开发/咨询/实施/产品迭代/运维增强），后端 400 硬校验，界面显示类型徽章，台账入口按类型动态命名（运维台账/课题表）
- **模板分类归一化**：项目模板按五分类管理，用模板建项目自动预填类型，存为模板自动带分类；非法分类 400、不传分类视为未分类
- **「产品迭代」升格为正式类型**（v1.8.7），并按用户口述完成 8 个存量有效项目的类型回填
- **系统模板扩至 5 个**（v1.8.8 / v1.8.9）：新增「咨询服务模板」（6 任务/2 预算/4 看板列）与「实施交付模板」（7 任务/2 预算/4 看板列），存量库启动按名称幂等自动补插

### 结项合并（v1.9.0）

- 仅「运维增强」类型项目可发起合并：台账整体转绑到目标运维项目（`movedOpLogs` 返回转移数量），源项目台账清零、目标项目可见，重复合并 400 拦截

### 工程与质量（v1.9.1）

- **e2e 集成测试**：`tests/e2e/cross-module.e2e.test.ts` 子进程启动真实服务（随机端口 + `FORTUNE_DB_PATH` 临时库 + SMTP 置空防外发），覆盖种子管理员登录/注册角色 → 项目类型硬校验 → 加成员 → 任务 → 工时 → 台账登记/列表/详情/统计 → 全局搜索 → 成员统计作用域 → 软删/回收站/恢复 共 13 项断言
- **CI 流水线**：`.github/workflows/ci.yml` 四道闸门（tsc → ESLint 0 警告 → vitest 全量含 e2e → 构建）；新增 `npm run test:e2e` 单独运行入口

---

## 🔧 改进

- **台账权限口径统一**（v1.9.1）：台账列表对全员开放（侧边栏入口 + 项目详情内嵌），此前 owner/member/guest 打开详情会被 403 拒绝；现已在三处同源补授 `oplog.view`——种子角色常量、运行时隐式权限映射（覆盖新注册用户）、`ensureSystemRolePermissions` 存量库启动幂等回填
- **ESLint 全量清零**（35 → 0）：`no-explicit-any` 9 项收窄为 `OpLog` 强类型；`react-hooks/exhaustive-deps` 18 项修复（派生数组 `useMemo` 固定引用、回调依赖补齐）；`ErrorFallback` 拆分独立组件文件并移除无引用的 `withErrorBoundary`
- **构建产物优化**：`manualChunks` 将 recharts / d3 / victory-vendor 拆为独立 `vendor-charts` chunk（362KB / gzip 89KB），与通用 `vendor`（406KB）分离，不使用图表的页面不再加载图表依赖

---

## 🐛 缺陷修复

- 🔴 **台账「看得到列表打不开详情」**：详情/AI 端点校验 `oplog.view` 而多数角色未授权（详见上「改进」）
- 咨询模板插入顺序缺陷（v1.8.8）：初版在 `initSystemTemplates` 之前补插导致新空库 count 守卫误跳过其余 3 个系统模板——已修正调用顺序并重放验证
- 日期正则 `no-useless-escape`、甘特图多余依赖等静态问题随 lint 清零一并修复

---

## ⚠️ 升级说明（必读）

**行为变更**（影响 API 直连方）：

1. `POST /api/projects` **强制要求 projectType**（六类白名单，非法值/缺省返回 400）；存量有效项目已由 v1.8.7 按口述回填，未设置类型的项目不显示徽章仍可正常使用
2. 仅「运维增强」类型项目支持合并到运维项目，其他类型发起合并返回 400
3. **权限矩阵变化**：owner/member/guest 系统角色默认新增「查看运维台账」（`oplog.view`）权限，启动时自动回填，无需手工干预；如需限制某角色的台账可见性，请在角色管理中调整自定义角色（列表仍对全员可见，为既定产品口径）
4. 台账详情不再对合法角色 403；未参与项目/越权记录仍按数据作用域隔离，安全性不回退

**数据兼容**：

- 无 schema 破坏，服务启动自动幂等迁移；系统模板（5 个）启动时按名称幂等补插，不与存量冲突
- 新增可选环境变量 `FORTUNE_DB_PATH` 用于覆盖数据库路径，**默认仍为 `data/app.db`，生产行为不变**（e2e 测试用临时库，生产数据零接触）

**版本元数据**：`package.json` 1.9.0 → **1.9.1**

---

## ✅ 发布验证

| 项目 | 结果 |
|------|------|
| 类型检查 `npx tsc --noEmit` | 0 错误 |
| ESLint `npx eslint .` | 0 错误 0 警告（35 → 0） |
| 单元/组件测试 `npx vitest run` | 139/139 |
| e2e 集成测试 `npm run test:e2e`（真实服务 + 临时库，13 项链路断言） | 13/13 |
| 构建 `npm run build` | 成功（vendor-charts 362KB / gzip 89KB 独立拆分） |
| 测试数据 | e2e 临时库/目录/子进程全部清理，生产 `data/app.db` 零残留（已回查确认） |

---

## 📚 相关文档

- [概要设计书](file:///Volumes/DATA/Project/docs/概要设计书.md)（12.1 演进记录 / 8.x 数据表口径）
- [操作手册](file:///Volumes/DATA/Project/docs/操作手册.md)（项目类型 / 结项合并 / 运维台账章节）
- [FEATURES.md](file:///Volumes/DATA/Project/FEATURES.md)（特性清单）
- [TEST_REPORT.md](file:///Volumes/DATA/Project/TEST_REPORT.md)（第二十 ~ 二十六章测试记录）
- [集成测试总结报告](file:///Volumes/DATA/Project/docs/测试总结报告-集成测试-20260922.md)
- [CI 流水线](file:///Volumes/DATA/Project/.github/workflows/ci.yml) / [e2e 测试](file:///Volumes/DATA/Project/tests/e2e/cross-module.e2e.test.ts)

---

*v1.10+ 规划：富文本评论、实时协作、移动端适配、自定义工作流*
