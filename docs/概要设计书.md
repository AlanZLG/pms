# Atlas 项目管理系统 · 概要设计书

> 版本 v1.1.0 · 编制日期 2026-07
> 状态:正式发布
> 变更:新增批量操作、@提及、任务模板、任务附件、邮件通知、CSV 导出、负责人指定、通知历史页

---

## 目录

1. [引言](#1-引言)
2. [系统概述](#2-系统概述)
3. [总体架构设计](#3-总体架构设计)
4. [功能模块设计](#4-功能模块设计)
5. [数据库设计](#5-数据库设计)
6. [API 接口设计](#6-api-接口设计)
7. [前端架构设计](#7-前端架构设计)
8. [安全设计](#8-安全设计)
9. [部署架构](#9-部署架构)
10. [技术选型说明](#10-技术选型说明)
11. [项目文件结构](#11-项目文件结构)
12. [演进规划](#12-演进规划)

---

## 1. 引言

### 1.1 编写目的

本文档为 Atlas 项目管理系统的概要设计说明书,旨在:

- 阐述系统的总体架构与设计思路
- 定义各功能模块的职责与边界
- 描述数据模型、API 接口与前端技术方案
- 为后续详细设计、编码实现与运维部署提供指导依据

### 1.2 系统目标

构建一款轻量级、开箱即用的团队项目管理 Web 应用,满足中小团队在日常协作中的核心需求:

- **看板驱动** —— 任务卡片在可视化看板中拖拽流转,状态变更实时同步
- **数据可见** —— 燃尽图、任务趋势、成员工作量等多维度统计
- **权限清晰** —— 四级角色体系,细粒度控制数据可见性与操作权限
- **协作闭环** —— 评论、子任务、通知构成完整的团队协作闭环

### 1.3 适用范围

本文档涵盖系统的后端服务、前端应用、数据存储、安全机制和部署架构的设计说明,不涉及具体实现代码的细节(详见源代码与操作手册)。

### 1.4 读者对象

- 项目经理 / 技术负责人:了解系统能力边界与架构选型
- 开发工程师:理解模块设计与接口规范,指导后续功能扩展
- 运维工程师:参考部署架构与运维指引

---

## 2. 系统概述

### 2.1 系统定位

Atlas 是一款面向中小团队的轻量级项目管理工具,采用前后端分离的 B/S 架构,通过单文件 SQLite 实现零配置启动,无需独立部署数据库服务。

### 2.2 核心能力矩阵

| 能力维度 | 具体能力 |
| --- | --- |
| **项目管理** | 项目创建 / 编辑 / 归档、进度自动计算、成员邀请与角色分配、创建时指定负责人 |
| **任务管理** | 任务 CRUD、状态流转、优先级与标签、截止日期、负责人指派 |
| **看板视图** | 四列看板(待办 / 进行中 / 审核中 / 已完成)、拖拽更新状态 |
| **列表视图** | 表格展示、多状态筛选、批量操作(改状态/改负责人/删除) |
| **子任务** | 检查清单、完成进度条、快速添加 |
| **评论系统** | 任务级评论、系统流水记录、@提醒(自动补全 + 通知) |
| **任务模板** | 模板 CRUD、从模板快速创建任务、保存当前配置为模板 |
| **任务附件** | 文件上传(最大 20MB)、下载、删除、文件类型图标 |
| **通知中心** | 铃铛入口、未读标记、单条/全部已读、独立通知历史页、类型筛选 |
| **邮件通知** | 任务完成自动发邮件(Nodemailer + SMTP)、HTML 模板 |
| **数据导出** | 项目列表 CSV 导出、项目任务 CSV 导出(UTF-8 BOM) |
| **数据统计** | 任务状态分布、14 天趋势、燃尽图、成员工作量 |
| **权限体系** | 系统管理员 / 项目负责人 / 团队成员 / 访客 四级角色 |
| **用户管理** | 注册、登录、JWT 鉴权、密码 bcrypt 加密 |

### 2.3 设计原则

1. **简洁优先**:零配置启动、开箱即用、单文件数据库
2. **类型安全**:前后端共享 TypeScript 类型定义,减少契约不一致
3. **渐进增强**:SPA 单页应用、响应式布局、移动端可用
4. **关注分离**:后端分层(路由 → 仓库 → 数据库)、前端分模块(页面 → 组件 → 状态)

---

## 3. 总体架构设计

### 3.1 架构风格

系统采用 **前后端分离 + 单体应用** 架构:

- **前端**:React 18 SPA,通过 HTTP/REST 与后端 API 通信
- **后端**:Express 单体服务,提供 JSON REST API
- **存储**:SQLite 单文件数据库,通过 better-sqlite3 驱动

### 3.2 架构总览图

```
┌─────────────────────────────────────────────────────────────────────┐
│                          用户浏览器                                  │
│                                                                     │
│  ┌───────────────────────────────────────────────────────────────┐  │
│  │                    React 18 SPA 前端应用                       │  │
│  │                                                               │  │
│  │  ┌─────────┐ ┌──────────┐ ┌────────┐ ┌─────────┐ ┌────────┐ │  │
│  │  │ 仪表板  │ │ 项目列表 │ │ 项目详情│ │  统计   │ │  团队  │ │  │
│  │  └────┬────┘ └────┬─────┘ └───┬────┘ └────┬────┘ └───┬────┘ │  │
│  │       │           │           │           │          │      │  │
│  │  ┌────┴───────────┴───────────┴───────────┴──────────┴────┐ │  │
│  │  │              路由层 (React Router)                       │ │  │
│  │  └─────────────────────────┬────────────────────────────────┘ │  │
│  │                            │                                   │  │
│  │  ┌─────────────────────────┴────────────────────────────────┐ │  │
│  │  │           全局状态 (Zustand Store)                        │ │  │
│  │  │   useAppStore · useNotificationStore                     │ │  │
│  │  └─────────────────────────┬────────────────────────────────┘ │  │
│  │                            │                                   │  │
│  │  ┌─────────────────────────┴────────────────────────────────┐ │  │
│  │  │              API 客户端 (api.ts)                         │ │  │
│  │  │    fetchTasks · createTask · updateTask · ...             │ │  │
│  │  └─────────────────────────┬────────────────────────────────┘ │  │
│  └────────────────────────────┼───────────────────────────────────┘  │
│                               │ HTTP / JSON                         │
└───────────────────────────────┼─────────────────────────────────────┘
                                │
┌───────────────────────────────┼─────────────────────────────────────┐
│                        Express.js 后端服务                           │
│                        (端口 3001)                                   │
│                               │                                     │
│  ┌────────────────────────────┴───────────────────────────────────┐  │
│  │                     中间件层                                    │  │
│  │  cors · json body parser · JWT 认证 (authRequired) · 错误处理    │  │
│  └────────────────────────────┬───────────────────────────────────┘  │
│                               │                                     │
│  ┌──────┐ ┌──────┐ ┌──────┐ ┌──────┐ ┌──────┐ ┌────────┐           │  │
│  │ auth │ │project│ │ task │ │stats │ │ team │ │notific.│  路由层   │  │
│  └──┬───┘ └──┬───┘ └──┬───┘ └──┬───┘ └──┬───┘ └───┬────┘           │  │
│     │        │        │        │        │        │                  │  │
│  ┌──┴────────┴────────┴────────┴────────┴────────┴──────────┐   │  │
│  │                    数据仓库层 (Repository)                    │   │  │
│  │  userRepo · projectRepo · taskRepo · commentRepo · ...       │   │  │
│  └─────────────────────────┬───────────────────────────────────┘   │  │
│                            │                                        │  │
│  ┌────────────────────────┴────────────────────────────────────┐   │  │
│  │                  better-sqlite3 (SQLite)                     │   │  │
│  │                      data/app.db                             │   │  │
│  └─────────────────────────────────────────────────────────────┘   │  │
│                                                                      │
└──────────────────────────────────────────────────────────────────────┘
```

### 3.3 数据流概览

```
用户操作 → React 组件事件 → Zustand Action → API 客户端
                                                        ↓
                                              HTTP POST / PATCH / GET
                                                        ↓
                                              Express 路由处理
                                                        ↓
                                              Zod 参数校验
                                                        ↓
                                              Repository 方法
                                                        ↓
                                              better-sqlite3 SQL 执行
                                                        ↓
                                              SQLite 文件读写
                                                        ↓
                                              返回 JSON 响应
                                                        ↓
                                              前端更新状态 → UI 重渲染
```

### 3.4 部署架构

```
┌─────────────────────────────────────────────────┐
│              应用服务器 (单机部署)                 │
│                                                 │
│  ┌─────────────────┐  ┌─────────────────────┐  │
│  │  前端静态资源    │  │  后端 Express 服务  │  │
│  │  (dist/ 目录)   │  │  (node server)     │  │
│  │  Vite 构建产物  │  │  端口 3001         │  │
│  └─────────────────┘  └─────────┬───────────┘  │
│                                  │              │
│                    ┌─────────────┴───────────┐  │
│                    │  SQLite 文件            │  │
│                    │  data/app.db            │  │
│                    └─────────────────────────┘  │
│                                                 │
└─────────────────────────────────────────────────┘
```

> 注:开发模式下前后端通过 Vite 代理或独立端口运行;生产模式下前端静态资源可由 Express 托管或部署至 CDN。

---

## 4. 功能模块设计

### 4.1 模块总览

```
Atlas 项目管理系统
├── M1 认证模块 (Authentication)
│   ├── 用户注册
│   ├── 用户登录
│   ├── Token 鉴权
│   └── 身份信息获取
│
├── M2 项目管理模块 (Project Management)
│   ├── 项目列表(搜索/筛选)
│   ├── 项目 CRUD
│   ├── 项目成员管理
│   ├── 创建时指定负责人(管理员)
│   └── 项目进度计算
│
├── M3 任务管理模块 (Task Management)
│   ├── 任务 CRUD
│   ├── 看板视图(拖拽)
│   ├── 列表视图(筛选 + 批量操作)
│   ├── 子任务管理
│   ├── 评论管理 + @提及
│   ├── 标签与优先级
│   └── 任务附件管理
│
├── M4 统计分析模块 (Analytics)
│   ├── 概览统计
│   ├── 任务状态分布
│   ├── 任务趋势(14天)
│   ├── 燃尽图
│   └── 成员工作量
│
├── M5 团队协作模块 (Team Collaboration)
│   ├── 成员列表
│   ├── 角色管理
│   └── 权限控制
│
├── M6 通知中心模块 (Notification)
│   ├── 通知列表(下拉 + 独立历史页)
│   ├── 未读计数
│   ├── 标记已读 / 删除
│   ├── 类型筛选
│   ├── @提及通知
│   └── 邮件通知(任务完成)
│
├── M7 任务模板模块 (Template)
│   ├── 模板 CRUD
│   ├── 从模板创建任务
│   └── 保存为模板
│
└── M8 数据导出模块 (Export)
    ├── 项目列表 CSV 导出
    └── 项目任务 CSV 导出
```

### 4.2 模块详细设计

#### 4.2.1 M1 认证模块

| 功能 | 输入 | 输出 | 说明 |
| --- | --- | --- | --- |
| 用户注册 | name, email, password | token, user | 首位注册用户自动成为 admin |
| 用户登录 | email, password | token, user | bcrypt 校验密码 |
| 获取身份 | Header: Authorization | user | JWT 解码 |
| Token 鉴权中间件 | 请求头 | AuthRequest | 解析 JWT,附加 userId |

**JWT Claims**

```json
{
  "sub": "user-uuid",
  "role": "admin",
  "iat": 1234567890,
  "exp": 1234654290
}
```

#### 4.2.2 M2 项目管理模块

**项目 CRUD**

| 操作 | API | 权限 |
| --- | --- | --- |
| 列表 | `GET /api/projects` | 管理员可见全部,其他仅可见参与的项目 |
| 详情 | `GET /api/projects/:id` | 已登录用户 |
| 创建 | `POST /api/projects` | 已登录用户,自动设为 owner;管理员可指定 ownerId |
| 更新 | `PATCH /api/projects/:id` | 项目 owner 或管理员 |
| 删除 | `DELETE /api/projects/:id` | 项目 owner 或管理员 |

**创建时指定负责人**:请求体增加可选 `ownerId` 字段,仅管理员可指定他人为负责人。指定后,该用户自动加入项目成员(role=owner)。非管理员尝试指定他人返回 403。

**成员管理**

| 操作 | API | 权限 |
| --- | --- | --- |
| 添加成员 | `POST /api/projects/:id/members` | 项目 owner 或管理员 |
| 修改角色 | `PATCH /api/projects/:id/members/:uid` | 项目 owner 或管理员 |
| 移除成员 | `DELETE /api/projects/:id/members/:uid` | 项目 owner 或管理员 |

**进度计算规则**

```
进度 = 已完成任务数 / 总任务数 × 100%
```

系统在每次任务创建、状态变更、删除时自动重算并更新 `projects.progress`。

#### 4.2.3 M3 任务管理模块

**看板视图**

四列固定:待办 → 进行中 → 审核中 → 已完成
拖拽流转:通过 `PATCH /api/tasks/:id/status` 更新状态

**列表视图与批量操作**

列表视图支持多选批量操作:
- 批量改状态:循环调用 `PATCH /api/tasks/:id/status`
- 批量改负责人:循环调用 `PATCH /api/tasks/:id`(传 assigneeId)
- 批量删除:循环调用 `DELETE /api/tasks/:id`

批量操作前端逐条调用现有接口,任一失败则停止并提示。

**子任务**

子任务作为任务的子资源,支持独立 CRUD:
- 创建:`POST /api/tasks/:id/subtasks`
- 更新:`PATCH /api/subtasks/:id`(支持修改标题和完成状态)
- 删除:`DELETE /api/subtasks/:id`

子任务完成度 = 已完成子任务数 / 总子任务数

**评论与系统流水**

系统在关键节点自动生成伪评论(以 `— ... —` 格式标识):
- 任务创建 → `— 创建了任务 —`
- 状态变更 → `— 将状态从「X」改为「Y」 —`
- 子任务增删 → `— 新增子任务:xxx —`

**@提及机制**

评论提交时,后端通过正则 `@用户名` 解析提及内容:
1. 提取所有 `@` 后的非空白字符串作为用户名
2. 通过 `userRepo.findByName` 查找对应用户
3. 为每个被提及的用户(排除自己)创建 comment 类型通知
4. 前端评论输入框监听 `@` 字符,弹出成员选择面板,支持键盘导航

**任务附件**

附件通过 multer 中间件处理上传:
- 上传:`POST /api/attachments/tasks/:taskId`(multipart/form-data,字段名 file)
- 列表:`GET /api/attachments/tasks/:taskId`
- 下载:`GET /api/attachments/:id`
- 删除:`DELETE /api/attachments/:id`(上传者或管理员)

文件存储于 `data/uploads/`,文件名为 UUID + 原扩展名,元数据(原文件名、大小、MIME 类型)存入 `attachments` 表。单文件最大 20MB。

#### 4.2.4 M7 任务模板模块

| 操作 | API | 权限 |
| --- | --- | --- |
| 列表 | `GET /api/templates` | 已登录用户 |
| 创建 | `POST /api/templates` | 已登录用户 |
| 删除 | `DELETE /api/templates/:id` | 已登录用户 |

模板字段:name(名称)、title(标题)、description(描述)、priority(优先级)、labels(标签数组)。前端在新建任务对话框提供"从模板创建"与"保存为模板"两个入口。

#### 4.2.5 M8 数据导出模块

| 操作 | API | 说明 |
| --- | --- | --- |
| 项目列表导出 | `GET /api/export/projects.csv` | 当前用户可见的项目 |
| 项目任务导出 | `GET /api/export/projects/:pid/tasks.csv` | 指定项目的全部任务 |

CSV 格式:UTF-8 编码,带 BOM 头(确保 Excel 中文正常),字段含中文状态/优先级标签。文件名使用 RFC 5987 编码处理中文。

#### 4.2.6 M4 统计分析模块

| 接口 | 说明 |
| --- | --- |
| `GET /api/stats/overview` | 全局概览:项目数、任务分布、14天趋势 |
| `GET /api/stats/burndown?projectId=xxx` | 指定项目的燃尽图数据 |
| `GET /api/stats/workload` | 成员工作量统计 |

**燃尽图算法**

```
理想剩余 = 总任务数 × (1 - 已过时间 / 总工期)
实际剩余 = 当前未完成任务数
```

总工期 = 项目创建时间 至 截止日期,按天计算。

#### 4.2.7 M5 团队协作模块

**四级角色权限矩阵**

| 功能 | admin | owner | member | guest |
| --- | --- | --- | --- | --- |
| 查看所有项目 | ✅ | ❌ | ❌ | ❌ |
| 查看参与项目 | ✅ | ✅ | ✅ | ✅ |
| 创建项目 | ✅ | ✅ | ✅ | ❌ |
| 编辑项目 | ✅ | ✅(自己的) | ❌ | ❌ |
| 管理项目成员 | ✅ | ✅(自己的) | ❌ | ❌ |
| 创建/编辑任务 | ✅ | ✅ | ✅(参与项目) | ❌ |
| 删除任务 | ✅ | ✅(自己的项目) | ❌ | ❌ |
| 修改成员角色 | ✅ | ❌ | ❌ | ❌ |
| 查看统计数据 | ✅ | ✅ | ✅ | ✅ |

#### 4.2.8 M6 通知中心模块

**通知类型**

| 类型 | 触发场景 |
| --- | --- |
| `assign` | 用户被分配新任务 / 解除分配 |
| `status` | 用户负责的任务状态变更 |
| `comment` | 用户负责的任务收到评论 / 评论中被 @提及 |
| `system` | 系统公告 |

**通知机制**

通知采用 **同步写入** 模式:后端在触发事件的 API 路由中直接写入 `notifications` 表,前端通过轮询(页面加载时)获取最新通知。

**通知历史页**

独立页面 `/notifications` 提供完整通知管理:
- 按类型筛选(全部/分配/状态/评论/系统),显示各类型数量
- 单条标记已读、删除
- 全部标记已读
- 点击通知跳转至关联项目/任务

**邮件通知**

任务状态变为 `done` 时,后端通过 Nodemailer 向负责人发送 HTML 邮件:
- 仅当负责人不是操作者本人时发送
- 邮件模板含品牌色渐变头部、项目名、任务标题
- SMTP 配置通过环境变量读取,未配置时自动跳过
- 邮件发送异步执行,不阻塞 API 响应

---

## 5. 数据库设计

### 5.1 数据库选型

选用 **SQLite**(better-sqlite3 驱动)作为数据存储,原因:

- 零配置:无需安装独立数据库服务
- 单文件:`data/app.db` 便于备份与迁移
- 轻量:适合中小规模数据(万级任务无压力)
- WAL 模式:支持并发读写
- 事务支持:保证数据一致性

### 5.2 ER 图

```
┌─────────────┐       ┌──────────────┐       ┌─────────────┐
│    users    │       │   projects   │       │ project_    │
├─────────────┤       ├──────────────┤       │  members    │
│ id (PK)     │◄──┐   │ id (PK)      │       ├─────────────┤
│ email (UQ)  │   │   │ name         │       │ id (PK)     │
│ password_   │   └───│ owner_id(FK) │       │ project_id  │
│   hash      │       │ status       │       │   (FK)      │
│ name        │       │ progress     │       │ user_id (FK)│
│ avatar_color│       │ due_date     │       │ role        │
│ role        │       │ created_at   │       │ joined_at   │
│ created_at  │       └──────┬───────┘       └─────────────┘
└──────┬──────┘              │                      │
       │                     │                      │
       │                     ▼                      │
       │              ┌──────────────┐              │
       │              │    tasks     │              │
       │              ├──────────────┤              │
       │              │ id (PK)      │◄─────────────┘
       │              │ project_id  │
       │              │   (FK)       │
       └─────────────►│ assignee_id  │
                      │   (FK, NULL) │
                      │ title        │
                      │ description  │
                      │ status       │
                      │ priority     │
                      │ labels(JSON) │
                      │ due_date     │
                      │ created_at   │
                      │ updated_at   │
                      └──────┬───────┘
                             │
          ┌──────────────────┼──────────────────┐
          │                  │                  │
          ▼                  ▼                  ▼
┌─────────────┐     ┌─────────────┐     ┌──────────────┐
│  comments   │     │  subtasks   │     │attachments   │
├─────────────┤     ├─────────────┤     ├──────────────┤
│ id (PK)     │     │ id (PK)     │     │ id (PK)      │
│ task_id(FK) │     │ task_id(FK) │     │ task_id (FK) │
│ user_id(FK) │     │ title       │     │ user_id (FK) │
│ content     │     │ done        │     │ filename     │
│ created_at  │     │ created_at  │     │ original_name│
└─────────────┘     └─────────────┘     │ size        │
                                        │ mime_type   │
                                        │ created_at  │
                                        └──────────────┘

┌─────────────┐     ┌─────────────────────────┐
│notifications│     │      templates          │
├─────────────┤     ├─────────────────────────┤
│ id (PK)     │     │ id (PK)                 │
│ user_id(FK) │     │ name                    │
│ type        │     │ title                   │
│ title       │     │ description             │
│ body        │     │ priority                │
│ task_id(FK) │     │ labels                  │
│ project_id  │     │ created_at              │
│ read        │     └─────────────────────────┘
│ created_at  │
└─────────────┘
```

### 5.3 数据表详细设计

#### 5.3.1 users — 用户表

| 字段 | 类型 | 约束 | 说明 |
| --- | --- | --- | --- |
| id | TEXT | PRIMARY KEY | UUID |
| email | TEXT | UNIQUE NOT NULL | 登录邮箱 |
| password_hash | TEXT | NOT NULL | bcrypt 哈希 |
| name | TEXT | NOT NULL | 昵称 |
| avatar_color | TEXT | NOT NULL DEFAULT '#6366F1' | 头像颜色 |
| role | TEXT | NOT NULL CHECK(role IN ('admin','owner','member','guest')) | 系统角色 |
| created_at | DATETIME | NOT NULL DEFAULT CURRENT_TIMESTAMP | 注册时间 |

#### 5.3.2 projects — 项目表

| 字段 | 类型 | 约束 | 说明 |
| --- | --- | --- | --- |
| id | TEXT | PRIMARY KEY | UUID |
| name | TEXT | NOT NULL | 项目名称 |
| description | TEXT | | 项目描述 |
| status | TEXT | NOT NULL CHECK(status IN ('planning','active','completed','archived')) | 项目状态 |
| owner_id | TEXT | NOT NULL REFERENCES users(id) | 项目负责人 |
| progress | INTEGER | NOT NULL DEFAULT 0 CHECK(progress BETWEEN 0 AND 100) | 完成进度 |
| due_date | DATETIME | | 截止日期 |
| created_at | DATETIME | NOT NULL DEFAULT CURRENT_TIMESTAMP | 创建时间 |

#### 5.3.3 project_members — 项目成员关联表

| 字段 | 类型 | 约束 | 说明 |
| --- | --- | --- | --- |
| id | TEXT | PRIMARY KEY | UUID |
| project_id | TEXT | NOT NULL REFERENCES projects(id) ON DELETE CASCADE | 项目 ID |
| user_id | TEXT | NOT NULL REFERENCES users(id) ON DELETE CASCADE | 用户 ID |
| role | TEXT | NOT NULL CHECK(role IN ('owner','editor','viewer')) | 项目内角色 |
| joined_at | DATETIME | NOT NULL DEFAULT CURRENT_TIMESTAMP | 加入时间 |

UNIQUE(project_id, user_id) 唯一约束

#### 5.3.4 tasks — 任务表

| 字段 | 类型 | 约束 | 说明 |
| --- | --- | --- | --- |
| id | TEXT | PRIMARY KEY | UUID |
| project_id | TEXT | NOT NULL REFERENCES projects(id) ON DELETE CASCADE | 项目 ID |
| title | TEXT | NOT NULL | 任务标题 |
| description | TEXT | | 详细描述 |
| status | TEXT | NOT NULL CHECK(status IN ('todo','in_progress','review','done')) | 任务状态 |
| priority | TEXT | NOT NULL CHECK(priority IN ('low','medium','high','urgent')) | 优先级 |
| assignee_id | TEXT | REFERENCES users(id) ON DELETE SET NULL | 负责人(可为空) |
| labels | TEXT | NOT NULL DEFAULT '' | JSON 数组序列化存储 |
| due_date | DATETIME | | 截止日期 |
| created_at | DATETIME | NOT NULL DEFAULT CURRENT_TIMESTAMP | 创建时间 |
| updated_at | DATETIME | NOT NULL DEFAULT CURRENT_TIMESTAMP | 更新时间 |

**索引**

- `idx_tasks_project` ON(project_id)
- `idx_tasks_assignee` ON(assignee_id)
- `idx_tasks_status` ON(status)

#### 5.3.5 comments — 评论表

| 字段 | 类型 | 约束 | 说明 |
| --- | --- | --- | --- |
| id | TEXT | PRIMARY KEY | UUID |
| task_id | TEXT | NOT NULL REFERENCES tasks(id) ON DELETE CASCADE | 任务 ID |
| user_id | TEXT | NOT NULL REFERENCES users(id) | 评论者 |
| content | TEXT | NOT NULL | 评论内容 |
| created_at | DATETIME | NOT NULL DEFAULT CURRENT_TIMESTAMP | 评论时间 |

**索引**: `idx_comments_task` ON(task_id)

#### 5.3.6 subtasks — 子任务表

| 字段 | 类型 | 约束 | 说明 |
| --- | --- | --- | --- |
| id | TEXT | PRIMARY KEY | UUID |
| task_id | TEXT | NOT NULL REFERENCES tasks(id) ON DELETE CASCADE | 父任务 ID |
| title | TEXT | NOT NULL | 子任务标题 |
| done | INTEGER | NOT NULL DEFAULT 0 | 0=未完成,1=已完成 |
| created_at | DATETIME | NOT NULL DEFAULT CURRENT_TIMESTAMP | 创建时间 |

**索引**: `idx_subtasks_task` ON(task_id)

#### 5.3.7 notifications — 通知表

| 字段 | 类型 | 约束 | 说明 |
| --- | --- | --- | --- |
| id | TEXT | PRIMARY KEY | UUID |
| user_id | TEXT | NOT NULL REFERENCES users(id) ON DELETE CASCADE | 接收者 |
| type | TEXT | NOT NULL | assign / status / comment / system |
| title | TEXT | NOT NULL | 通知标题 |
| body | TEXT | | 通知正文 |
| task_id | TEXT | | 关联任务 |
| project_id | TEXT | | 关联项目 |
| read | INTEGER | NOT NULL DEFAULT 0 | 0=未读,1=已读 |
| created_at | DATETIME | NOT NULL DEFAULT CURRENT_TIMESTAMP | 创建时间 |

**索引**

- `idx_notifications_user` ON(user_id)
- `idx_notifications_read` ON(read)

#### 5.3.8 templates — 任务模板表

| 字段 | 类型 | 约束 | 说明 |
| --- | --- | --- | --- |
| id | TEXT | PRIMARY KEY | UUID |
| name | TEXT | NOT NULL | 模板名称 |
| title | TEXT | NOT NULL | 预设任务标题 |
| description | TEXT | NOT NULL DEFAULT '' | 预设任务描述 |
| priority | TEXT | NOT NULL DEFAULT 'medium' CHECK(priority IN ('low','medium','high','urgent')) | 预设优先级 |
| labels | TEXT | NOT NULL DEFAULT '' | 预设标签(逗号分隔) |
| created_at | DATETIME | NOT NULL DEFAULT CURRENT_TIMESTAMP | 创建时间 |

#### 5.3.9 attachments — 任务附件表

| 字段 | 类型 | 约束 | 说明 |
| --- | --- | --- | --- |
| id | TEXT | PRIMARY KEY | UUID |
| task_id | TEXT | NOT NULL REFERENCES tasks(id) ON DELETE CASCADE | 所属任务 |
| user_id | TEXT | NOT NULL REFERENCES users(id) | 上传者 |
| filename | TEXT | NOT NULL | 存储文件名(UUID + 扩展名) |
| original_name | TEXT | NOT NULL | 原始文件名 |
| size | INTEGER | NOT NULL | 文件大小(字节) |
| mime_type | TEXT | NOT NULL | MIME 类型 |
| created_at | DATETIME | NOT NULL DEFAULT CURRENT_TIMESTAMP | 上传时间 |

**索引**
- `idx_attachments_task` ON(task_id)
- `idx_attachments_user` ON(user_id)

文件本体存储于 `data/uploads/` 目录,文件名为 UUID + 原扩展名,避免重名冲突。

### 5.4 数据完整性约束

- **级联删除**:项目删除时,其下的任务、成员关联自动删除;用户删除时,其负责的任务置 NULL、关联自动删除
- **外键约束**: `PRAGMA foreign_keys = ON`
- **CHECK 约束**:所有枚举字段(role、status、priority)均通过 CHECK 约束保证值域合法
- **唯一约束**:同一用户不可重复加入同一项目

---

## 6. API 接口设计

### 6.1 接口规范

- **协议**:HTTP REST
- **数据格式**:JSON (UTF-8)
- **认证方式**:Bearer Token (JWT)
- **统一响应**:成功返回业务数据,错误返回 `{ error: string }`
- **错误码**

| HTTP 状态码 | 含义 |
| --- | --- |
| 200 OK | 请求成功 |
| 201 Created | 资源创建成功 |
| 400 Bad Request | 参数校验失败 |
| 401 Unauthorized | 未登录或 Token 无效 |
| 403 Forbidden | 无权限操作 |
| 404 Not Found | 资源不存在 |
| 409 Conflict | 资源冲突(如重复注册) |

### 6.2 认证接口

| Method | Path | 说明 | 认证 |
| --- | --- | --- | --- |
| POST | `/api/auth/register` | 用户注册 | ❌ |
| POST | `/api/auth/login` | 用户登录 | ❌ |
| GET | `/api/auth/me` | 获取当前用户信息 | ✅ |

**注册请求体**
```json
{ "name": "张三", "email": "zhangsan@demo.com", "password": "123456" }
```

**注册/登录响应**
```json
{
  "token": "eyJhbGciOiJIUzI1NiIs...",
  "user": {
    "id": "uuid",
    "email": "zhangsan@demo.com",
    "name": "张三",
    "avatarColor": "#6366F1",
    "role": "member",
    "createdAt": "2025-01-01T00:00:00Z"
  }
}
```

### 6.3 项目接口

| Method | Path | 说明 | 认证 |
| --- | --- | --- | --- |
| GET | `/api/projects` | 项目列表 | ✅ |
| GET | `/api/projects/:id` | 项目详情 | ✅ |
| POST | `/api/projects` | 创建项目 | ✅ |
| PATCH | `/api/projects/:id` | 更新项目 | ✅ |
| DELETE | `/api/projects/:id` | 删除项目 | ✅ |
| POST | `/api/projects/:id/members` | 添加成员 | ✅ |
| PATCH | `/api/projects/:id/members/:uid` | 修改成员角色 | ✅ |
| DELETE | `/api/projects/:id/members/:uid` | 移除成员 | ✅ |

**创建项目请求体**
```json
{
  "name": "新项目名称",
  "description": "项目描述(可选)",
  "status": "planning",
  "dueDate": "2025-12-31T00:00:00Z"
}
```

### 6.4 任务接口

| Method | Path | 说明 | 认证 |
| --- | --- | --- | --- |
| GET | `/api/projects/:pid/tasks` | 项目下任务列表 | ✅ |
| POST | `/api/projects/:pid/tasks` | 创建任务 | ✅ |
| GET | `/api/tasks/:id` | 任务详情(含评论/子任务) | ✅ |
| PATCH | `/api/tasks/:id` | 更新任务 | ✅ |
| PATCH | `/api/tasks/:id/status` | 仅更新状态(看板拖拽) | ✅ |
| DELETE | `/api/tasks/:id` | 删除任务 | ✅ |
| POST | `/api/tasks/:id/comments` | 发表评论 | ✅ |
| POST | `/api/tasks/:id/subtasks` | 创建子任务 | ✅ |
| PATCH | `/api/subtasks/:id` | 更新子任务 | ✅ |
| DELETE | `/api/subtasks/:id` | 删除子任务 | ✅ |

**创建任务请求体**
```json
{
  "title": "实现登录页面",
  "description": "包含邮箱、密码字段",
  "status": "todo",
  "priority": "high",
  "assigneeId": "user-uuid",
  "labels": ["前端", "UI"],
  "dueDate": "2025-08-01T00:00:00Z"
}
```

**看板状态更新请求体**
```json
{ "status": "in_progress" }
```

### 6.5 模板接口

| Method | Path | 说明 | 认证 |
| --- | --- | --- | --- |
| GET | `/api/templates` | 模板列表 | ✅ |
| POST | `/api/templates` | 创建模板 | ✅ |
| DELETE | `/api/templates/:id` | 删除模板 | ✅ |

**创建模板请求体**
```json
{
  "name": "Bug 修复模板",
  "title": "[Bug] 修复 xxx 问题",
  "description": "复现步骤:\n1. ...\n预期:\n实际:",
  "priority": "high",
  "labels": ["Bug", "后端"]
}
```

### 6.6 附件接口

| Method | Path | 说明 | 认证 |
| --- | --- | --- | --- |
| GET | `/api/attachments/tasks/:taskId` | 任务附件列表 | ✅ |
| POST | `/api/attachments/tasks/:taskId` | 上传附件(multipart) | ✅ |
| GET | `/api/attachments/:id` | 下载附件 | ✅ |
| DELETE | `/api/attachments/:id` | 删除附件 | ✅ |

**上传附件**:请求类型为 `multipart/form-data`,字段名 `file`,单文件最大 20MB。

### 6.7 导出接口

| Method | Path | 说明 | 认证 |
| --- | --- | --- | --- |
| GET | `/api/export/projects.csv` | 导出项目列表 CSV | ✅ |
| GET | `/api/export/projects/:pid/tasks.csv` | 导出项目任务 CSV | ✅ |

CSV 响应头:`Content-Type: text/csv; charset=utf-8`,带 BOM 头,文件名通过 RFC 5987 编码处理中文。

### 6.8 统计接口

| Method | Path | 说明 | 认证 |
| --- | --- | --- | --- |
| GET | `/api/stats/overview` | 全局概览数据 | ✅ |
| GET | `/api/stats/burndown?projectId=xxx` | 燃尽图数据 | ✅ |
| GET | `/api/stats/workload` | 成员工作量 | ✅ |

**概览响应示例**
```json
{
  "totalProjects": 4,
  "activeProjects": 2,
  "completedProjects": 1,
  "overdueProjects": 0,
  "tasksByStatus": { "todo": 5, "in_progress": 3, "review": 1, "done": 8 },
  "trend": [
    { "date": "2025-07-14", "completed": 2, "created": 3 },
    { "date": "2025-07-15", "completed": 1, "created": 0 }
  ]
}
```

### 6.9 团队接口

| Method | Path | 说明 | 认证 |
| --- | --- | --- | --- |
| GET | `/api/team/users` | 获取成员列表(含统计) | ✅ |
| PATCH | `/api/team/users/:id/role` | 修改成员角色 | ✅(仅 admin) |

### 6.10 通知接口

| Method | Path | 说明 | 认证 |
| --- | --- | --- | --- |
| GET | `/api/notifications` | 获取通知列表 + 未读数 | ✅ |
| GET | `/api/notifications/unread` | 仅获取未读数量 | ✅ |
| POST | `/api/notifications/:id/read` | 标记单条已读 | ✅ |
| POST | `/api/notifications/read-all` | 全部标记已读 | ✅ |
| DELETE | `/api/notifications/:id` | 删除单条通知 | ✅ |

### 6.11 错误响应格式

所有业务错误统一返回:
```json
{ "error": "任务不存在" }
```

系统异常返回:
```json
{ "error": "Internal Server Error" }
```

---

## 7. 前端架构设计

### 7.1 技术选型

| 分类 | 技术 | 版本 | 选型理由 |
| --- | --- | --- | --- |
| 框架 | React | 18.3 | 生态成熟、社区活跃 |
| 构建 | Vite | 6.x | 开发体验好、构建快 |
| 语言 | TypeScript | 5.8 | 类型安全,减少契约不一致 |
| 路由 | React Router DOM | 7.x | 官方推荐方案 |
| 样式 | TailwindCSS | 3.x | 原子化 CSS、开发高效 |
| 状态 | Zustand | 5.x | 轻量、API 简洁 |
| 拖拽 | @dnd-kit/core | 6.x | 现代化拖拽方案、支持移动端 |
| 图表 | Recharts | 2.x | 基于 D3 的 React 图表库 |
| 表单 | React Hook Form | 7.x | 性能优异、类型安全 |
| 图标 | lucide-react | 0.511+ | 现代化图标库 |

### 7.2 目录结构

```
src/
├── App.tsx                  # 路由配置 + 全局初始化
├── main.tsx                 # 入口文件
├── index.css                # TailwindCSS 入口 + 主题变量
├── components/
│   ├── AppLayout.tsx        # 主布局(侧边栏 + 顶栏)
│   ├── TaskDrawer.tsx       # 任务详情抽屉
│   ├── TaskDialog.tsx       # 任务创建/编辑对话框
│   ├── ProjectDialog.tsx    # 项目创建/编辑对话框
│   ├── NotificationDropdown.tsx  # 通知下拉面板
│   ├── Toaster.tsx          # 全局 Toast 通知
│   └── ui/                  # 通用 UI 组件
│       ├── Card.tsx
│       ├── Button.tsx
│       ├── Input.tsx
│       ├── Textarea.tsx
│       ├── Avatar.tsx
│       ├── PriorityBadge.tsx
│       ├── StatusBadge.tsx
│       ├── LabelTag.tsx
│       ├── Skeleton.tsx
│       └── EmptyState.tsx
├── pages/
│   ├── Login.tsx            # 登录/注册页
│   ├── Dashboard.tsx        # 仪表板
│   ├── Projects.tsx         # 项目列表
│   ├── ProjectDetail.tsx    # 项目详情(看板/列表 + 批量操作)
│   ├── Stats.tsx            # 统计分析
│   ├── Team.tsx             # 团队协作
│   ├── Notifications.tsx    # 通知历史页(筛选/删除)
│   └── Settings.tsx         # 个人设置
├── stores/
│   ├── app.ts               # 全局应用状态(用户、主题等)
│   └── notifications.ts     # 通知状态管理
├── lib/
│   ├── api.ts               # API 客户端封装
│   ├── date.ts              # 日期工具函数
│   └── utils.ts             # 通用工具(cn 等)
└── hooks/
    └── useAsync.ts          # 异步数据加载 Hook
```

### 7.3 状态管理

#### 7.3.1 useAppStore

```typescript
interface AppState {
  user: User | null
  initialized: boolean
  loading: boolean
  
  init(): Promise<void>
  login(email, password): Promise<void>
  register(name, email, password): Promise<void>
  logout(): void
  notify(level, message): void
}
```

#### 7.3.2 useNotificationStore

```typescript
interface NotificationState {
  notifications: Notification[]
  unread: number
  
  fetch(): Promise<void>
  markRead(id): Promise<void>
  markAllRead(): Promise<void>
}
```

### 7.4 路由设计

| 路径 | 组件 | 认证要求 | 说明 |
| --- | --- | --- | --- |
| `/login` | Login | ❌ | 登录页 |
| `/register` | Login(mode='register') | ❌ | 注册页 |
| `/` | Dashboard | ✅ | 仪表板(默认首页) |
| `/projects` | Projects | ✅ | 项目列表 |
| `/projects/:projectId` | ProjectDetail | ✅ | 项目详情 |
| `/stats` | Stats | ✅ | 统计页 |
| `/team` | Team | ✅ | 团队页 |
| `/notifications` | Notifications | ✅ | 通知历史页 |
| `/settings` | Settings | ✅ | 个人设置 |
| `*` | Redirect('/') | ✅ | 通配符重定向 |

### 7.5 主题设计

系统采用深色主题,核心设计 Token 如下:

| Token | 值 | 用途 |
| --- | --- | --- |
| `--brand` | `#6366F1` | 品牌主色(靛蓝) |
| `--brand-soft` | `#A5B4FC` | 品牌浅色 |
| `--ok` | `#10B981` | 成功/已完成 |
| `--warn` | `#F59E0B` | 警告/进行中 |
| `--danger` | `#EF4444` | 错误/逾期 |
| `--bg` | `#0F172A` | 主背景 |
| `--bg-soft` | `#1E293B` | 卡片背景 |
| `--bg-panel` | `#172036` | 面板背景 |
| `--text` | `#E2E8F0` | 主文字 |
| `--muted` | `#94A3B8` | 次要文字 |

---

## 8. 安全设计

### 8.1 密码安全

- 密码使用 **bcrypt**(cost factor = 10)加密存储,不可逆
- 注册时进行密码强度校验(最少 6 位)
- 登录失败不区分"邮箱不存在"与"密码错误",防止枚举攻击

### 8.2 会话安全

- 使用 **JWT**(HS256)作为会话令牌
- Token 有效期:24 小时
- Token 存储于 localStorage,每次请求通过 `Authorization: Bearer <token>` 头携带
- 服务端通过 `authRequired` 中间件校验 Token 有效性

### 8.3 权限控制

| 层面 | 实现方式 |
| --- | --- |
| **路由层** | `authRequired` 中间件确保所有 API(除登录/注册外)需认证 |
| **数据层** | 项目列表按角色过滤:管理员可见全部,其他仅可见参与项目 |
| **操作层** | 创建/更新/删除操作校验项目 owner 或管理员身份 |
| **前端 UI** | 按钮/下拉框根据角色禁用或隐藏(如"仅管理员可修改角色") |

### 8.4 输入校验

所有写操作均使用 **Zod** schema 校验,确保:
- 字段类型正确(如 email 格式、日期格式)
- 字段长度限制(如标题 ≤ 120 字符)
- 枚举值合法(如 status 必须为四种之一)
- 必填字段不为空

### 8.5 CORS 策略

开发模式下允许所有来源(`cors()`),生产环境应配置具体域名白名单。

### 8.6 SQL 注入防护

使用 better-sqlite3 的 **参数化查询**(prepared statement),所有用户输入均通过 `?` 占位符绑定,无拼接 SQL 风险。

### 8.7 文件上传安全

- **大小限制**:multer 中间件限制单文件最大 20MB,防止大文件耗尽磁盘
- **文件名处理**:存储文件名使用 UUID + 原扩展名,避免路径穿越攻击
- **类型记录**:记录 MIME 类型与原始文件名,但不限制文件类型(灵活支持各类文档)
- **权限校验**:下载与删除附件需登录认证,删除需为上传者本人或管理员

---

## 9. 部署架构

### 9.1 开发环境

```bash
# 前置条件
- Node.js >= 18
- npm

# 安装依赖
npm install

# 启动(推荐,同时启动前后端)
npm run dev

# 前端:http://localhost:5173
# 后端:http://localhost:3001
```

### 9.2 生产部署

```bash
# 构建前端
npm run build
# 产物:dist/ 目录

# 后端以生产模式运行
# 方式一:直接启动
node api/app.js

# 方式二:nodemon(生产环境也可用)
npm run server:dev
```

**环境变量配置**

```bash
# .env 文件(参考 .env.example)
JWT_SECRET=your-production-secret-key-here
PORT=3001

# 邮件通知(可选,未配置则跳过邮件)
SMTP_HOST=smtp.example.com
SMTP_PORT=587
SMTP_USER=your-email@example.com
SMTP_PASS=your-smtp-auth-code
SMTP_FROM=Atlas <your-email@example.com>
```

### 9.3 数据备份

```bash
# SQLite 文件级备份
cp data/app.db data/app.db.backup

# 附件文件备份(如有)
cp -r data/uploads data/uploads.backup

# 定时备份(cron 示例)
# 每天凌晨 3 点备份
0 3 * * * cp /path/to/data/app.db /path/to/backup/app.db.$(date +\%Y\%m\%d)
```

### 9.4 进程管理(可选)

生产环境建议使用 PM2 或 systemd 管理 Node 进程:

```bash
# PM2 示例
pm2 start "node api/app.js" --name atlas-api
pm2 save
```

### 9.5 Nginx 反向代理(可选)

```nginx
server {
    listen 80;
    server_name your-domain.com;

    # 前端静态资源
    location / {
        root /path/to/dist;
        try_files $uri /index.html;
    }

    # 后端 API 代理
    location /api/ {
        proxy_pass http://127.0.0.1:3001;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
    }
}
```

---

## 10. 技术选型说明

### 10.1 后端

| 技术 | 选型理由 |
| --- | --- |
| Express 4 | Node.js 生态最成熟的 Web 框架,中间件丰富,社区活跃 |
| better-sqlite3 | 同步 API、高性能、零配置。相比异步的 sqlite3 更简单可靠 |
| JWT (jsonwebtoken) | 无状态认证,适合 SPA 场景,无需服务端维护 Session |
| bcrypt | 业界标准的密码哈希方案,抗彩虹表攻击 |
| Zod | TypeScript 原生支持,运行时类型安全,与前后端类型共享无缝集成 |
| multer | Express 文件上传中间件,支持 multipart/form-data,配置灵活 |
| Nodemailer | Node.js 事实标准邮件库,支持 SMTP,HTML 模板,生态成熟 |

### 10.2 前端

| 技术 | 选型理由 |
| --- | --- |
| React 18 | 函数式组件 + Hooks,生态最成熟,招聘与维护成本低 |
| Vite 6 | 闪电般的冷启动、HMR,开发体验远超 Webpack |
| TailwindCSS 3 | 原子化 CSS,无需切换文件,设计一致性强 |
| Zustand 5 | 相比 Redux 更简洁,无需 Provider,TypeScript 支持优秀 |
| @dnd-kit/core | 现代化的拖拽库,支持触摸、键盘,API 设计优雅 |
| Recharts 2 | 基于 D3 构建,React API 友好,覆盖常见图表类型 |
| React Hook Form 7 | 相比 Formik 性能更好,使用 Controller 模式与受控组件集成 |
| lucide-react | 开源图标库,设计现代、一致,Tree-shake 友好 |

### 10.3 数据库

SQLite 适合本系统的原因:
- **规模匹配**:中小团队(≤100 人)使用,数据量在万级
- **运维简单**:单文件部署,无需 DBA
- **性能足够**:单用户场景下读写并发无压力
- **备份方便**:直接拷贝文件即可

未来如需要支持更大规模,可平滑迁移至 PostgreSQL/MySQL。

---

## 11. 项目文件结构

```
Project/
├── api/                          # 后端代码
│   ├── app.ts                   # Express 应用配置
│   ├── index.ts                 # 入口(Vercel handler)
│   ├── db.ts                    # 数据库连接 + 建表 + 种子数据
│   ├── lib/
│   │   ├── auth.ts              # JWT 签发/校验、authRequired 中间件
│   │   ├── mail.ts              # 邮件发送(Nodemailer + SMTP)
│   │   └── utils.ts             # 工具函数(genId、ApiError 等)
│   ├── repository/
│   │   └── repo.ts              # 数据仓库层(各实体 CRUD)
│   └── routes/
│       ├── auth.ts              # /api/auth/* 认证路由
│       ├── projects.ts          # /api/projects/* 项目路由
│       ├── tasks.ts             # /api/projects/:pid/tasks/* 任务路由
│       ├── stats.ts             # /api/stats/* 统计路由
│       ├── team.ts              # /api/team/* 团队路由
│       ├── notifications.ts     # /api/notifications/* 通知路由
│       ├── templates.ts         # /api/templates/* 模板路由
│       ├── attachments.ts       # /api/attachments/* 附件路由
│       └── export.ts            # /api/export/* CSV 导出路由
│
├── src/                          # 前端代码
│   ├── App.tsx                  # 路由配置
│   ├── main.tsx                 # React 入口
│   ├── index.css                # 全局样式 + Tailwind
│   ├── components/              # 组件(见 7.2 节)
│   ├── pages/                   # 页面(见 7.2 节)
│   ├── stores/                  # Zustand 状态(见 7.3 节)
│   ├── lib/                     # 工具库(见 7.2 节)
│   └── hooks/                   # 自定义 Hooks(见 7.2 节)
│
├── shared/                       # 前后端共享
│   └── types.ts                 # 类型定义(User、Project、Task 等)
│
├── data/                         # SQLite 数据库 + 附件(运行时生成)
│   ├── app.db
│   └── uploads/                 # 任务附件文件存储
│
├── docs/                         # 文档
│   ├── 操作手册.md               # 用户操作手册
│   └── 概要设计书.md             # 本文档
│
├── .env.example                  # 环境变量模板
├── package.json                  # 项目配置
├── vite.config.ts                # Vite 配置
├── tsconfig.json                 # TypeScript 配置
├── tailwind.config.ts            # Tailwind 配置
├── postcss.config.js             # PostCSS 配置
└── .env                          # 环境变量(不入库)
```

---

## 12. 演进规划

### 12.1 当前版本 (v1.1.0)

- ✅ 四大核心模块(看板 / 统计 / 项目 / 团队)
- ✅ 用户认证与权限体系
- ✅ 通知中心(下拉 + 独立历史页 + 类型筛选)
- ✅ 子任务/检查清单
- ✅ 响应式设计(桌面 + 移动端)
- ✅ 批量操作(改状态 / 改负责人 / 删除)
- ✅ @提及功能(评论自动补全 + 通知)
- ✅ 任务模板(CRUD + 从模板创建 + 保存为模板)
- ✅ 任务附件(上传 / 下载 / 删除,最大 20MB)
- ✅ 任务完成邮件通知(Nodemailer + SMTP)
- ✅ CSV 导出(项目列表 + 项目任务)
- ✅ 项目创建时指定负责人(管理员)
- ✅ 环境变量模板(.env.example)

### 12.2 近期规划 (v1.2 ~ v1.3)

| 优先级 | 功能 | 说明 |
| --- | --- | --- |
| P0 | 自定义角色 | 支持自定义角色与细粒度权限配置 |
| P1 | 富文本评论 | 评论支持 Markdown / 富文本编辑 |
| P1 | 任务回收站 | 软删除 + 回收站恢复,防误删 |
| P2 | 附件图片预览 | 图片类型附件支持在线预览 |
| P2 | 通知实时推送 | WebSocket 替代轮询,实时通知 |

### 12.3 中长期规划 (v2.0+)

| 方向 | 功能 |
| --- | --- |
| 协作增强 | 实时协作(WebSocket)、富文本评论、多人光标 |
| 文档管理 | 项目文档、版本历史、Wiki |
| 流程自动化 | 自定义工作流、触发器、定时任务 |
| 集成生态 | GitHub/GitLab 集成、Webhook、API 开放平台 |
| 多语言 | 中/英双语支持 |
| 数据迁移 | 支持 PostgreSQL/MySQL 替换 SQLite |
| 权限模型升级 | 自定义角色、细粒度权限配置、字段级权限 |

---

*Atlas 项目管理系统 · 概要设计书 v1.1.0 · END*
