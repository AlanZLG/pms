# Fortune 项目管理系统 — 功能清单

## 一、认证与用户

| 功能 | 说明 |
|------|------|
| 注册/登录 | 邮箱+密码注册登录，JWT Token 认证 |
| 个人信息 | 获取/更新当前用户资料 |
| 飞书集成 | OAuth 绑定/解绑/回调，飞书账号关联 |
| 限流保护 | 注册 5次/分、登录 10次/分，全局限流 |

## 二、项目管理

| 功能 | 说明 |
|------|------|
| 项目 CRUD | 创建、查看、编辑、归档项目 |
| 成员管理 | 添加成员、修改角色（owner/editor/viewer）、移除成员 |
| 批量导入 | CSV 文件批量导入项目成员，提供模板下载 |
| 看板列配置 | 自定义看板列（label/color/sortOrder），支持拖拽排序 |
| 项目模板 | 将现有项目（含任务/预算/看板列）保存为模板，从模板创建新项目；**模板分类（v1.8.6）**：运维/开发/咨询/实施/产品迭代/运维增强六类必选（v1.9.0），创建项目选模板按分类自动预填同名项目类型，模板下拉按分类分组；**五个系统模板**：网站开发（开发）/产品迭代（产品迭代）/客户支持（运维）/咨询服务（咨询，v1.8.8）/实施交付（实施，v1.8.9） |

## 三、任务管理

| 功能 | 说明 |
|------|------|
| 任务 CRUD | 创建、查看、编辑、删除任务 |
| 三种视图 | 看板视图（拖拽改状态）、列表视图、甘特图视图（点击任务名行内编辑计划/实际日期与进度；计划虚线条+实际绿条对比，进度数值常显） |
| 状态流转 | todo → in_progress → review → done，支持拖拽切换 |
| 子任务 | 创建/编辑/删除/勾选子任务 |
| 任务依赖 | 建立任务间依赖关系，支持阻塞/被阻塞标记 |
| 评论 | 任务内评论讨论 |
| 附件 | 文件上传/下载/删除 |
| 操作历史 | 完整审计日志，记录所有状态变更 |
| 搜索 | 全局任务搜索（标题/描述/标签） |
| 高级筛选 | 按状态/优先级/指派人/标签/日期筛选，可保存筛选方案 |
| 软删除 | 回收站机制，支持恢复和物理删除 |
| 任务模板 | 保存常用任务为模板，快速创建 |

## 四、工时管理

| 功能 | 说明 |
|------|------|
| 工时记录 | 按任务记录日期/计划工时/内部实际工时/计费工时/备注 |
| 工时报表 | 任务计划工时（任务设置）与登记计划工时（工时登记）双口径卡片 |
| 筛选体系 | 项目/人员/日期范围服务端筛选，人员筛选时提示未指派任务的计划工时；默认期间当月、默认项目为本人负责的第一个项目，人员下拉随项目联动（成员优先 + 自动补齐有登记/负责任务的历史参与人，同名加邮箱前缀消歧） |
| 工时偏差 | 偏差 = 内部实际合计 − 任务计划合计，偏差率按任务计划为分母（分母 0 时显示 0%） |
| 趋势图 | 登记计划（蓝柱）/ 登记实际（绿柱）按日聚合，叠加任务计划基线（黄线，按任务起止区间均摊，仅区间 ≤60 天的任务参与） |
| 消耗进度 | 已消耗内部工时 / 任务计划工时进度条，超 100% 红色告警 |
| 个人工时 | 查询当前用户的工时记录（GET /api/hours/me） |
| 工时统计 | 按用户/项目维度汇总（项目核算权限） |
| 项目成本 | 基于工时×时薪计算项目人力成本（批量 SQL 汇总，无笛卡尔积放大）；时薪回退链：个人时薪 → 类别时薪 → 项目角色默认价（负责人 200/成员 150/外包 125，按项目内实际角色动态取价）→ 0 |
| 报表滚动布局 | 人员-项目矩阵双向滚动（表头吸顶/人员列吸左/合计行吸底）、明细与成本区块纵向滚动、人员工时统计与趋势图等高内滚 |

## 五、预算与费用

| 功能 | 说明 |
|------|------|
| 预算管理 | 按项目创建预算项（人力/外包/硬件/软件/其他） |
| 审批流程 | 项目核算审批（待审批→批准/驳回） |
| 费用管理 | 项目实际费用录入与管理 |
| 预算汇总 | 项目预算总额 vs 实际花费对比 |

## 六、统计与报表

| 功能 | 说明 |
|------|------|
| 数据可见范围 | 除系统管理员外，仅统计自己参与（owner 或成员）的项目；越权指定项目返回空集 |
| 仪表盘 | 进行中/已完成/逾期项目数、任务总量、任务状态分布（全部按可见范围过滤） |
| 时间范围联动 | 统计页右上角 7/14/30 天切换，状态分布/燃尽图/成员工作量/项目成本全指标联动（overview/burndown/workload 显式传 days 启用窗口口径，不传保持全量，仪表板不受影响） |
| 燃尽图 | 项目剩余任务趋势（支持可见范围过滤）；days 窗口切片：时间轴延伸到今天并只返回最近 N+1 天，理想线锚定不变 |
| 工作负载 | 成员任务分配情况（非管理员仅列自己与可见项目成员）；按 updated_at 窗口过滤（有新建或更新的任务），图表纵向滚动、柱高自适应人数 |
| 14天趋势 | 近期任务创建/完成趋势（支持可见范围过滤） |
| 项目进度 | 各项目进度百分比一览 |
| 数据导出 | 任务 CSV、项目 CSV、进度 Excel |

## 七、团队管理

| 功能 | 说明 |
|------|------|
| 成员列表 | 全系统成员一览；接口按 team.view 权限分级：admin/finance/owner 返回完整通讯录（邮箱+任务统计），member/guest 仅返回协作最小字段（姓名/头像，供负责人下拉） |
| 角色管理 | 系统角色（admin/finance/owner/member/guest）+ 自定义角色 |
| 成本设置 | 成员时薪配置（项目核算权限）；未设置时按回退链取价：类别时薪 → 项目角色默认价（负责人 200/成员 150/外包 125） |
| 用户分类 | 自定义用户分类标签管理 |

## 八、权限系统

| 功能 | 说明 |
|------|------|
| 自定义角色 | 创建/编辑/删除自定义角色 |
| 权限矩阵 | 按分类列出所有权限项，为角色分配权限 |
| 角色绑定 | 将自定义角色绑定到团队成员 |
| 数据作用域 | scopedProjectIds/resolveScope 统一解析可见项目集合：admin 不限制、普通用户=参与项目、无参与项目=强制空集（绝不回退全量） |

## 九、通知系统

| 功能 | 说明 |
|------|------|
| 实时推送 | 长轮询机制（25s 超时），新通知即时推送 |
| 通知管理 | 列表/未读数/标记已读/全部已读/删除 |
| 角铃提醒 | 顶栏通知铃铛，实时未读数 |

## 十、系统功能

| 功能 | 说明 |
|------|------|
| 全局搜索 | ⌘K 快捷搜索任务/项目 |
| 暗色模式 | 明暗主题切换 |
| 回收站 | 查看/恢复/彻底删除已软删除任务；项目删除审批待办区块（管理员/项目核算可批准或驳回） |
| 模板管理 | 项目模板 + 任务模板的完整管理 |
| 数据备份 | 数据库备份/恢复，备份信息查看 |
| 数据库维护 | WAL checkpoint、VACUUM 压缩 |

## 十一、运维台账/课题表（v1.8.4 更名：运维项目登记台账，开发实施项目记录课题）

| 功能 | 说明 |
|------|------|
| 台账记录 | 13 个固定字段 + 自定义扩展字段，按项目 Tab 隔离（全部/项目/未关联） |
| 筛选与搜索 | 分类/状态/系统/部门/日期区间/关键词（多字段防抖搜索） |
| **列排序** | 点击表头（日期/状态/完成日/工时）切换升/降序；状态按业务顺序排（待处理→处理中→已完成→已关闭）；后端白名单防注入，非法字段回退默认 |
| **问题/方案列阅读** | 长文本默认 2 行折叠，点击展开/收起；单元格内一键复制全文 |
| **只读详情** | 无编辑权限（oplog.edit）的用户可打开台账详情查看全文，表单只读、AI 分析隐藏 |
| Excel 导入 | 智能表头识别、状态中英文映射、多日期格式、单行失败跳过 |
| Excel 导出 | 与列表同筛选同排序，状态列着色，跨项目导出附加项目列 |
| 统计 | 月份 × 分类交叉表（条数/工时合计） |
| 任务关联 | 任务转台账（TaskToOpLog），来源任务可溯源 |
| **AI 经验分析（v1.6）** | 配置 OpenAI 兼容 LLM 后，按当前问题召回已闭环台账的相似案例，流式（SSE）生成原因分析/建议步骤/风险提示；相似案例可点击直接打开对应台账详情 |
| **语义召回（v1.6，可选）** | 双路召回（候选集 + 关键词 LIKE 全文扩展）；配置 `LLM_EMBED_MODEL` 后叠加向量余弦重排，向量按台账缓存（`op_log_embeddings` 表），内容变更/删除/换模型自动失效；embedding 失败静默降级为关键词排序 |
| **AI 排障助手（v1.7）** | 台账页对话式问答：右侧抽屉多轮流式对话，每轮自动检索台账相似案例注入上下文并展示可点击的参考案例卡片；支持停止生成、多轮追问；无状态会话（刷新即清空，服务端不落库） |
| **召回分词修复（v1.7.1）** | 关键词提取改用 Node 内置 `Intl.Segmenter` 词典级分词（中文切词/英文按单词）+ 停用词表，自然语言提问不再召回恒空（修复前无标点整句被当单个关键词必不命中，AI 只能给通用建议） |
| **台账数据作用域（v1.8）** | 非管理者只能查看/操作「自己创建或参与的项目」的台账（与统计同口径），admin 不限；覆盖列表/统计/选项/导出/详情/增删改/导入，越权读写返回 403/404、越权查询返回空集防枚举；无项目归属台账仅 admin 可见；AI 案例召回不隔离（跨项目经验共享），不可见案例卡片点击有明确提示 |
| **台账导入查重（v1.8.1）** | Excel 导入时按「项目 + 作业日期 + 问题全文（换行/空白归一化）」自动查重：选择文件后先**预检**（不写库），发现重复弹窗确认——跳过重复导入新记录 / 全部照导 / 取消，弹窗逐行列出重复明细；无重复时直接导入；台账页缺省 Tab 改为自动选中第一个项目 |
| **台账经验字段必填（v1.8.2）** | 状态为「已完成」时「详细描述/原因/解决方案」必填：表单红星+保存校验、后端新建/PATCH 双拦截（PATCH 合并现有值校验防清空）；Excel 导入缺字段行照常导入但预检与结果逐行提醒补填 |
| **台账必须关联项目（v1.8.3）** | 移除「未关联」视图，所有运维作业（含内部作业/内部学习）强制归属项目名下归集工时：表单无「不关联」选项+保存校验，导入必须先选具体项目，后端新建/导入强制 projectId（缺失 400、项目不存在 404）；历史无归属数据仍仅 admin 可见 |
| **更名与未识别列提醒（v1.8.4）** | 功能更名「运维台账/课题表」（导航/页标题/项目 Tab/任务转换/导出模板文件名同步，文件名用下划线规避斜杠非法）；Excel 导入预检与结果响应新增未识别列名（`unknownColumns`，最多 10 个），弹窗与通知琥珀色提醒防标准列名拼错静默偏差，模板"自定义列示例"列豁免 |
| **项目类型（v1.8.5）** | 新建项目必选项目类型（表单首位按钮组，未选不放行）；后端 create 强校验、update 可改；项目卡片与详情展示类型徽章（历史未设置不显示）；台账入口按类型动态命名——运维项目显示「运维台账」，其余类型显示「课题表」 |
| **项目类型五类（v1.8.7）** | 类型在四类基础上新增「产品迭代」并升格为正式类型（五色徽章、五选一按钮）；与模板分类同名一一对应；存量有效项目已按实际用途回填类型 |
| **项目类型六类（v1.9.0）** | 类型扩为六类（新增「运维增强」：基于现有系统的功能追加/改善开发，橙色徽章、六选一按钮）；台账入口规则扩展——**运维项目/运维增强→运维台账**，其余→课题表（`projectLedgerLabel` 统一封装）；模板分类同步六类同名对应 |
| **结项合并（v1.9.0）** | 「运维增强」项目结项后**一键合并到运维项目**：详情页「合并到运维项目」→ 事务内台账/课题记录批量转绑（工时随之归集）→ 原项目标记青色「已合并 → 目标」徽章；强校验：仅运维增强可合并、不可重复、目标必须为运维项目且未合并、不得合并到自身；合并不可撤销 |

## 技术架构概要

- **前端**：React 18 + TypeScript + Vite + TailwindCSS + dnd-kit（拖拽）
- **后端**：Express 4 + TypeScript + better-sqlite3
- **认证**：JWT Token，中间件鉴权（authRequired / financeRequired）
- **实时**：HTTP 长轮询（非 WebSocket）
- **AI**：OpenAI 兼容协议薄封装（`lib/llm.ts`：chat / chatStream（空闲超时）/ embed），召回评分、分词提取（Intl.Segmenter）与语义重排纯函数化（`lib/recall.ts`），SSE 公共解析（`readSseEvents`）
- **数据模型**：42 个类型定义，覆盖用户/项目/任务/预算/通知/权限全链路
- **API 路由**：15 个路由文件，约 100 个接口端点
- **测试**：Vitest 单元测试 139 项（数据权限作用域、工时口径、LLM 流式/向量解析、召回评分与分词回归、台账数据作用域、导入查重键、对话 Prompt 与 AI 助手组件交互、项目类型归一化与台账入口、模板分类等），`npx vitest run`
