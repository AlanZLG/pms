# 版本发布说明 · v1.8.4

> **Fortune-PMS 项目管理系统** · 发布日期 2026-09-22
> 版本跨度：v1.6.0 ~ v1.8.4（上一发布版本 v1.5.0）
> 质量基线：`tsc` 0 错误 · 单元/组件测试 **124/124** · 模块结合集成测试 **53/53**

---

## 🌟 版本亮点

1. **运维台账 AI 能力落地**：相似案例召回 + SSE 流式分析、对话式排障助手（多轮、可溯源、可停止），并修复中文召回分词缺陷
2. **台账模块治理闭环（v1.8.x 四连击）**：数据作用域隔离 → 导入查重弹窗 → 经验字段必填 → 必须关联项目
3. **功能更名「运维台账/课题表」**：一套数据双用途——运维项目登记台账，开发实施项目记录课题
4. **导入防呆增强**：预检未识别列提醒，杜绝标准列名拼错的静默偏差
5. **修复新装库工时登记缺陷**（`task_hours.billed_hours` 缺列）

---

## ✨ 新增功能

### AI 智能排障（v1.6 ~ v1.7）

- 运维台账相似案例召回（关键词 + 可选 embedding 语义重排），AI 经验分析流式输出，失败自动降级非流式
- 台账页对话式排障助手：多轮 SSE 流式对话、参考案例卡片溯源（点击只读查看）、停止生成、案例列表展开/收起
- 修复中文分词召回缺陷（Intl.Segmenter 词典级分词 + 停用词表），自然语言提问不再「召回恒空」

### 统计与工时（v1.5 ~ v1.6）

- 统计页 7/14/30 天全指标联动；工时报表整改：计划工时/登记工时双口径、偏差分析、趋势基线、矩阵双向滚动布局
- 成员成本默认时薪回退链（按项目角色动态定价）

### 运维台账/课题表治理（v1.8.x）

| 版本 | 能力 |
|------|------|
| v1.8 | 数据作用域隔离：非管理者仅见自己创建/参与项目的台账，覆盖全部端点；越权读写 403/404、越权查询空集防枚举 |
| v1.8.1 | 导入查重：选择文件先预检（不写库），重复行弹窗确认——跳过重复 / 全部照导 / 取消 |
| v1.8.2 | 经验字段必填：「已完成」状态强制填写详细描述/原因/解决方案（表单校验 + 后端双拦截 + 防清空）；导入缺字段照常导入并逐行提醒 |
| v1.8.3 | 台账必须关联项目：移除「未关联」视图，新建/导入强制所属项目，内部作业统一归集到项目名下 |
| v1.8.4 | 功能更名「运维台账/课题表」（界面 + 导出/模板文件名）；导入未识别列提醒 |

---

## 🐛 缺陷修复

- 🔴 **新装库工时登记失败**：`task_hours` 建表缺 `billed_hours` 列（存量库靠迁移未暴露），已修复
- SSE 客户端断开检测误杀流式响应（req→res close 语义修正）
- 中文召回分词缺陷（见上）
- 成本汇总 SQL 统计口径修复
- 甘特图日期闭区间显示偏差

---

## ⚠️ 升级说明（必读）

**破坏性变更**（影响 API 直连方 / Excel 模板习惯）：

1. `POST /api/op-logs` 与 Excel 导入**强制要求 projectId**：不传返回 400「台账必须关联项目」；导入前须先指定目标项目
2. 「已完成」台账的新建/编辑缺经验字段（详细描述/原因/解决方案）将被 400 拒绝（PATCH 合并校验，不可清空已填字段）
3. Excel 导入缺省**跳过重复行**（项目+作业日期+问题相同）；`skipDuplicates=false` 才全部照导
4. 界面与文档中「运维台账」更名「运维台账/课题表」，导出文件名变更为 `运维台账_课题表_日期.xlsx`（文件名不含斜杠）

**数据兼容**：

- 无 schema 破坏，服务启动自动幂等迁移；历史无项目归属台账保留（仅 admin 可见）
- AI 召回不隔离项目（跨项目经验共享为设计决策），不可见案例卡片点击有明确提示

**版本元数据**：`package.json` 1.5.0 → **1.8.4**

---

## ✅ 发布验证

| 项目 | 结果 |
|------|------|
| 类型检查 `npx tsc --noEmit` | 0 错误 |
| 单元/组件测试 `npx vitest run` | 124/124 |
| 模块结合集成测试（独立库 + 真实服务，10 大链路 53 项检查） | 53/53 |
| 导入未识别列冒烟（误写列名提示 / 模板列豁免） | 2/2 |
| 测试数据 | 全部清除，生产数据零残留 |

---

## 📚 相关文档

- [概要设计书](file:///Volumes/DATA/Project/docs/概要设计书.md)（12.1 演进记录 / 8.x 数据表口径）
- [操作手册](file:///Volumes/DATA/Project/docs/操作手册.md)（第 25 章 运维台账/课题表）
- [FEATURES.md](file:///Volumes/DATA/Project/FEATURES.md)（特性清单）
- [TEST_REPORT.md](file:///Volumes/DATA/Project/TEST_REPORT.md)（第十 ~ 十九章测试记录）
- [集成测试总结报告](file:///Volumes/DATA/Project/docs/测试总结报告-集成测试-20260922.md)

---

*v1.9+ 规划：富文本评论、实时协作、移动端适配、自定义工作流*
