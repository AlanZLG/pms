# Fortune-PMS 测试报告

> 📌 最新测试轮次：**技术债清零与 e2e/CI 沉淀（2026-09-23，v1.9.1）**，见「二十六、技术债清零与 e2e/CI 沉淀」。以下一~九章为 v1.1.0 轮次的历史记录，第十~二十五章为后续回归与功能测试，均保留备查。

**版本**: v1.1.0
**测试日期**: 2026-07-30
**测试范围**: 本轮 10 项改进 + 3 项 Bug 修复
**测试环境**: macOS 26.5.2 / Node.js v22.22.3 / better-sqlite3

---

## 一、测试结果总览

| 类别 | 用例数 | 通过 | 失败 | 通过率 |
|------|--------|------|------|--------|
| 编译构建 | 4 | 4 | 0 | 100% |
| 数据库 | 3 | 3 | 0 | 100% |
| API 端点 | 9 | 9 | 0 | 100% |
| 端到端功能 | 7 | 7 | 0 | 100% |
| 前端静态校验 | 10 | 10 | 0 | 100% |
| **合计** | **33** | **33** | **0** | **100%** |

**总体结论**: ✅ 全部通过，可发布

---

## 二、编译与构建测试

### 2.1 前端 TypeScript 严格检查
```
命令: npx tsc --noEmit
结果: ✅ 通过 (0 errors)
```

### 2.2 项目类型检查
```
命令: npm run check
结果: ✅ 通过 (0 errors)
```

### 2.3 Vite 生产构建
```
命令: npm run build
结果: ✅ 通过 (3115 modules transformed, 2.61s)
产物:
  - index.html              0.87 kB (gzip 0.51 kB)
  - index-*.css            37.96 kB (gzip 7.20 kB)
  - index-*.js          1,111.83 kB (gzip 261.23 kB)  ← 主包
  - Stats-*.js             24.50 kB (gzip 7.24 kB)     ← 懒加载分块
  - HoursReport-*.js       63.00 kB (gzip 7.50 kB)     ← 懒加载分块
  - BarChart-*.js           0.30 kB (gzip 0.24 kB)     ← recharts 共享分块
```

### 2.4 ESLint 代码规范
```
命令: npm run lint
结果: ⚠️ 171 个历史问题 (exit 0, 不阻断构建)
  - 改动文件新增问题: 0 个新增错误
  - 全部为项目既有的 no-explicit-any / no-empty / unused-vars
```

---

## 三、数据库 Schema 验证

### 3.1 tasks 表 progress 字段
```
✅ 字段存在: progress INTEGER NOT NULL DEFAULT 0
✅ CHECK 约束: progress BETWEEN 0 AND 100
✅ 迁移逻辑: 旧库自动 ALTER TABLE 添加字段
```

### 3.2 task_history 表（新增）
```
✅ 表已创建，字段:
  - id (TEXT PRIMARY KEY)
  - task_id (TEXT, FK → tasks)
  - user_id (TEXT, FK → users)
  - action (TEXT)
  - detail (TEXT)
  - created_at (DATETIME)
✅ 索引: idx_history_task, idx_history_user
✅ 级联删除: ON DELETE CASCADE
```

### 3.3 迁移幂等性
```
✅ 多次启动不报错,迁移代码包裹在 try/catch 中
✅ 已存在字段检测: pragma('table_info') 前置判断
```

---

## 四、API 端点连通性测试

### 4.1 认证与基础
| 端点 | 方法 | 状态 | 说明 |
|------|------|------|------|
| `/api/auth/login` | POST | ✅ 200 | 登录成功返回 token |
| `/api/auth/me` | GET | ✅ 200 | 鉴权正常 |
| `/api/notifications` | GET | ✅ 200 | 通知列表正常 |

### 4.2 统计路由（原 /api/overview 已迁移）
| 端点 | 方法 | 状态 | 说明 |
|------|------|------|------|
| `/api/stats/overview` | GET | ✅ 200 | 统计概览 |
| `/api/stats/workload` | GET | ✅ 200 | 成员工作量 |

### 4.3 任务与项目
| 端点 | 方法 | 状态 | 说明 |
|------|------|------|------|
| `/api/projects` | GET | ✅ 200 | 项目列表 |
| `/api/projects/:id/tasks` | GET | ✅ 200 | 项目任务 |
| `/api/tasks/:id` | GET | ✅ 200 | 任务详情（含 history） |
| `/api/tasks/:id` | PATCH | ✅ 200 | 任务更新 |
| `/api/tasks/nonexistent` | GET | ✅ 404 | 正确返回 404 |

### 4.4 导出端点
| 端点 | 方法 | 状态 | Content-Type |
|------|------|------|---------------|
| `/api/export/projects.csv` | GET | ✅ 200 | text/csv |
| `/api/export/projects/:id/tasks.csv` | GET | ✅ 200 | text/csv |
| `/api/export/projects/:id/progress.xlsx` | GET | ✅ 200 | application/vnd.openxmlformats... |
| `/api/export/projects.csv` (无 token) | GET | ✅ 401 | 未授权拦截正常 |

---

## 五、端到端功能测试

### 5.1 Bug #1 — Progress 持久化
```
步骤: PATCH /api/tasks/:id { progress: 75 } → GET /api/tasks/:id
预期: 返回 progress = 75
实际: ✅ 0% → 75% 持久化成功
```

### 5.2 Progress 边界值校验
```
✅ progress=150 → HTTP 400 (zod 拦截)
✅ progress=-10 → HTTP 400 (zod 拦截)
✅ progress=50  → HTTP 200 (正常更新)
```

### 5.3 Bug #3 — 项目进度自动重算
```
步骤: 将项目所有任务 progress 设为 100% → 查询项目 progress
预期: 项目 progress 接近 100%
实际: ✅ 13% → 100%
算法: MAX(AVG(task.progress), done_count/total*100)
```

### 5.4 #9 — TaskHistory 记录
```
步骤: 多次 PATCH 任务进度 → GET /api/tasks/:id
预期: 返回 history 数组,记录所有变更
实际: ✅ 历史记录数: 2
  - progress_update: 进度: 50%
  - progress_update: 进度: 75%
```

---

## 六、前端改进点静态校验

| # | 改进项 | 验证结果 |
|---|--------|----------|
| 1 | 状态常量统一 | ✅ constants.ts 导出 6 个共享常量,4 个组件已接入 |
| 2 | 路由懒加载代码分割 | ✅ Stats/HoursReport 独立分块,recharts 共享分块 |
| 3 | 甘特图依赖线视口裁剪 | ✅ clipPath="url(#dep-clip)" 已实现 |
| 4 | 甘特图键盘快捷键 | ✅ +/-/D/G/Esc 快捷键已绑定 |
| 5 | 甘特图任务搜索框 | ✅ searchQuery 状态 + 搜索 UI 已实现 |
| 6 | API GET 请求重试 | ✅ maxRetries 机制已实现,5xx 重试 4xx 不重试 |
| 7 | 依赖线箭头样式 | ✅ 三种箭头: arrowhead / arrowhead-active / arrowhead-dep |
| 8 | 视图切换过渡动画 | ✅ key={view} + animate-fade-up 已添加 |
| 9 | TaskHistory | ✅ 后端 historyRepo 已接入,API 返回 history |
| 10 | 表单实时验证 | ✅ 标题长度/日期范围实时校验已实现 |

---

## 七、性能基线

| 指标 | 改进前 | 改进后 | 变化 |
|------|--------|--------|------|
| 主包 gzip | 276.83 kB | 261.23 kB | ↓ 15.6 kB (-5.6%) |
| Stats 独立分块 | 0 (含在主包) | 7.24 kB | 按需加载 |
| HoursReport 独立分块 | 0 (含在主包) | 7.50 kB | 按需加载 |
| 构建时间 | 2.68s | 2.61s | ↓ 0.07s |
| 模块数 | 3113 | 3115 | +2 (constants.ts 等) |

---

## 八、已知限制与建议

### 8.1 已知限制
1. **主包仍含 recharts**: Dashboard 是首屏路由(/),需直接渲染图表,无法懒加载。当前主包 gzip 261KB 可接受。→ **已拆分（2026-09-23，v1.9.1）**：`manualChunks` 将 recharts/d3/victory-vendor 拆为独立 `vendor-charts` chunk（361KB/gzip 89KB），与通用 vendor 分离
2. **Lint 历史问题**: 项目累计 171 个 ESLint 警告,均为既有的 `no-explicit-any` / `no-empty` / 未使用变量,不影响功能。建议后续批量整改。→ **已清零（2026-09-23，v1.9.1）**：经多轮整改后剩余 35 项（any 9 / exhaustive-deps 18 / 未使用变量 5 / 无用转义 2 / react-refresh 1）全部清零
3. **未引入自动化测试框架**: 项目尚无 Jest/Vitest 测试套件,本轮测试为手动 + 脚本化验证。→ 已解决（后续轮次引入 Vitest，见第十~二十六章）

### 8.2 后续建议
- ~~引入 Vitest + React Testing Library 建立单元测试基线~~ → 已实施（当前 152 项）
- 对 progress 持久化、TaskHistory 记录编写集成测试 → 已由 subtask-status 等用例覆盖
- ~~用 `manualChunks` 进一步拆分 recharts 到独立 vendor chunk~~ → 已实施（2026-09-23，v1.9.1）
- ~~批量清理 ESLint `no-explicit-any` 警告~~ → 已实施（2026-09-23，v1.9.1，含 exhaustive-deps 等全部规则清零）

---

## 九、测试结论

**本轮 13 项改动（3 Bug 修复 + 10 改进）全部通过验证**:

- ✅ 编译零错误,构建成功
- ✅ 数据库迁移幂等,新表/新字段就位
- ✅ 9 个 API 端点连通正常,鉴权/导出/404 处理正确
- ✅ Progress 持久化端到端验证通过,边界值校验生效
- ✅ 项目进度自动重算准确
- ✅ TaskHistory 记录正确写入与返回
- ✅ 10 项前端改进点静态校验全部就位
- ✅ 主包体积下降 5.6%,代码分割生效

**可发布状态**: 🟢 就绪

## 十四、召回关键词分词缺陷修复测试（2026-09-21）

**背景**: 用户反馈 AI 回答「像模板不像 AI」。排查确认 LLM API 调用链路完全正常（vLLM 端点 HTTP 200、chat-stream 真实流式输出），真正的根因在召回层：`extractKeywords` 只按标点切分，无标点的自然语言整句要么超 20 字上限被整体丢弃（关键词为空 → 召回直接返回空），要么整句作为一个关键词去 LIKE（必然不命中）——导致几乎任何提问都空召回，模型永远回答「未检索到 + 通用建议」。

### 14.1 修复内容

| 项 | 修复前 | 修复后 |
|----|--------|--------|
| 分词方式 | 按标点/空白切分片段（2~20 字） | Node 内置 `Intl.Segmenter('zh-CN')` 词典级分词（中文切词、英文按单词），零依赖 |
| 过滤规则 | 长度 2~20 | 单字/纯数字/停用词（怎么/是否/需要等 27 个虚词与泛词）过滤 |
| 关键词上限 | 6 个 | 8 个 |
| 附带 | — | `tsconfig.json` lib 增加 `ES2022.Intl`（Intl.Segmenter 类型） |

### 14.2 自动化测试

```
命令: npx tsc --noEmit          → ✅ 0 错误
命令: npx vitest run            → ✅ 100/100 passed (基线 99 → 100)
```

recall.test.ts 中 extractKeywords 3 项按新行为重写 + 新增 1 项关键回归（无标点长句「月结时Sharepoint上报表合计不一致怎么处理」必须提取出 报表/合计/Sharepoint 且不含停用词、不包含整句）。

### 14.3 修复前后冒烟对比（同一问题实测）

| 指标 | 修复前 | 修复后 |
|------|--------|--------|
| meta 召回案例数 | 0（恒为空） | 8 |
| 回答行为 | 永远「未检索到相似案例 + 通用建议」 | 分析案例相关性，引用台账编号（如 f7381422 主数据生效日期），给出结合台账业务语境的建议 |

**可发布状态**: 🟢 就绪

## 十五、运维台账数据作用域隔离测试（2026-09-21）

**需求**: 非管理者只能打开与本人相关项目（创建 + 参与，与统计模块同口径）的台账，其余不显示；仅 admin 可见全部。
**范围**: 列表 / 统计 / 筛选选项 / 导出 xlsx / 详情 / 新增 / 修改 / 删除 / 导入全部台账端点 + repo 层 `projectIds` 集合过滤 + 前端 AI 案例卡片跨项目兜底提示。AI 案例召回按约定**不隔离**（跨项目经验共享）。

### 15.1 自动化测试

```
命令: npx tsc --noEmit          → ✅ 0 错误
命令: npx vitest run            → ✅ 114/114 passed (基线 100 → 114)
```

新增 tests/op-log-scope.test.ts（14 项）：scopeToProjectIds 换算（admin 不限 / 越权显式查询 → null / 空集合）、repo find/stats/distinct 的 projectIds 过滤（含空集合短路、projectId 与 projectIds 叠加）、与 scopedProjectIds 组合的路由行为模拟（成员只见参与项目 / 防越权枚举 / admin 全量 / 无项目用户空集）。

### 15.2 真实冒烟（member1@pm.dev vs admin 实测）

| 场景 | 结果 |
|------|------|
| member1 台账列表（不筛选） | ✅ 1 条（与 DB 交叉核对一致；全库 99 条） |
| admin 台账列表 | ✅ 99 条（全量不变） |
| member1 显式查无关项目详情 | ✅ 403 拒绝（member 角色未授权 `oplog.view`，由既有权限中间件拦截；scope 层 404 逻辑由单测覆盖） |
| member1 新建无项目归属台账 | ✅ 403「请关联你有权限的项目」 |
| member1 新建到无关项目台账 | ✅ 403「无权在该项目下登记台账」 |
| member1 stats / options / export | ✅ 200，数据已限定可见项目集合 |

### 15.3 说明

1. **无项目归属（project_id 为空）的台账仅 admin 可见**——与「只有系统管理者可以看到全部」口径一致（当前库中该类记录为 0 条，无存量影响）
2. **既有权限矩阵差异**（非本次引入）：列表/统计/导出端点不校验 `oplog.view`（登录即可，数据已由作用域限制），详情端点校验——未授权 `oplog.view` 的角色会出现「看得到列表、打不开详情」的割裂，如需统一可后续在权限矩阵为相应角色授权 → **已统一（2026-09-23，v1.9.1）**：台账列表对全员开放（侧边栏入口 + 项目详情内嵌），故在权限矩阵为 owner/member/guest 补授 `oplog.view`（种子角色常量 + 运行时隐式映射 + `ensureSystemRolePermissions` 存量库幂等回填），列表与详情口径一致
3. **AI 案例召回不隔离**：跨项目案例卡片点击时服务端 404，前端提示「该案例所在项目不在你的可见范围内」

**可发布状态**: 🟢 就绪

## 十六、台账导入查重测试（2026-09-21）

**需求背景**: 排查确认项目「预实BAS系统」下存在 3 组 × 8 条完全重复台账（重复导入所致，共 24 条 / 21 条冗余），为防止再次发生，导入功能增加查重提示。

### 16.1 实现

- 查重键：**项目 + 作业日期 + 问题全文**（`\r\n→\n` 归一化、去首尾空白）
- 导入端点拆为**解析 / 写入两阶段**，支持 `mode=preview` 预检（只解析统计、不写库）
- 前端流程：选择文件 → 预检 → **无重复直接导入**；**发现重复弹出确认窗口**（逐行列出重复明细），三选：「跳过重复，导入新记录」/「全部照导」/「取消导入」（confirm 模式 `skipDuplicates=false` 支持照导）
- 导入结果含 `duplicates` 明细与 `duplicateCount`，前端按重复有无分级提示
- 附带体验修正：台账页缺省 Tab 由「未关联」（常为空列表）改为自动选中第一个项目
- 测试基建：notify 支持 warning 类型（Toaster 增加警示图标与配色）

### 16.2 自动化测试

```
命令: npx tsc --noEmit          → ✅ 0 错误
命令: npx vitest run            → ✅ 119/119 passed (基线 114 → 119)
```

新增 tests/op-log-dup-key.test.ts（5 项）：归一化、同问题不同日期不算重复、跨项目不误拦、归一化后一致判重、空日期同值键。

### 16.3 真实冒烟（三种模式，5 行测试 Excel：2 行与库重复 + 3 行新记录）

| 步骤 | 结果 |
|------|------|
| 预检模式（mode=preview） | ✅ total=5、duplicateCount=2、newCount=3、dupRows=[2,3]（含 CRLF 归一化行）；**库不变（30 条）** |
| 照导模式（skipDuplicates=false） | ✅ imported=5（重复行照导），库 30 → 35 |
| 默认导入（跳过重复） | ✅ imported=3、duplicateCount=2，库恢复 30 |
| 数据清理 | ✅ 测试记录全部清理，项目台账数恢复 30，无残留 |

**可发布状态**: 🟢 就绪

---

## 十七、台账经验字段必填测试（2026-09-21，v1.8.2）

**需求背景**: 所有运维台账都应填写「详细描述 / 原因 / 解决方案」以沉淀排障经验。确认口径：仅状态为「已完成」时必填（待处理/处理中可先登记后补齐）；Excel 导入缺字段的行警告放行。

### 17.1 实现

- 公共校验函数 `completionMissing`（opLogs.ts 导出）：已完成状态返回缺失字段名列表，非已完成返回空——POST / PATCH / 导入三处共用口径
- 新建（POST）：已完成缺字段 → 400 列出缺失项
- 更新（PATCH）：**合并现有值与本次更新后校验**，防止把已完成记录的经验字段清空
- 导入：已完成缺字段行**照常导入**，返回 `fieldWarnings` / `fieldWarningCount`；预检响应亦含 `fieldWarningCount`（弹窗中提醒）
- 前端：状态为已完成时三个字段 label 显示红色 *，保存前校验并提示

### 17.2 自动化测试

```
命令: npx tsc --noEmit          → ✅ 0 错误
命令: npx vitest run            → ✅ 124/124 passed (基线 119 → 124)
```

新增 tests/oplog-completion.test.ts（5 项）：非已完成状态不强制（含 status 缺省/null）、已完成全空列出三项、部分缺失只列缺失项、纯空白视为未填、PATCH 合并场景（更新清空已完成记录字段被拦截）。

### 17.3 真实冒烟

| 场景 | 预期 | 实际 |
|------|------|------|
| 新建已完成缺原因/解决方案 | 400 | ✅ 400「状态为已完成时必须填写：原因、解决方案」 |
| 新建已完成三字段齐全 | 201 | ✅ 201 |
| PATCH 清空已完成记录的原因 | 400 | ✅ 400「状态为已完成时必须填写：原因」 |
| 待处理缺三字段 | 201 不拦 | ✅ 201 |
| 导入 2 行（1 行已完成缺字段 + 1 行完整） | imported=2、fieldWarnings=1 | ✅ 逐行提醒「已完成但缺少：原因、解决方案」 |
| 数据清理 | 恢复 30 条 | ✅ 项目台账数恢复 30，无残留 |

**可发布状态**: 🟢 就绪

---

## 十八、台账必须关联项目测试（2026-09-22，v1.8.3）

**需求背景**: 所有运维作业（含内部作业、内部学习）都必须归属到项目名下，作业内容与工时统一归集到项目统计。拍板移除「未关联」虚拟 Tab，并将「台账必须关联项目」落为前后端硬校验。

### 18.1 实现

- 前端 OpLogsPanel：移除「未关联」Tab 与表单「不关联项目」选项；全局模式「全部台账」视图下点导入提示「请先选择具体项目，再导入该项目的台账」；handleSave 校验 projectId 非空（「台账必须关联项目，请选择所属项目」）；payload 不再回退 null
- 后端 POST /op-logs：强制 `b.projectId` 必填（缺失/空 → 400「台账必须关联项目，请选择所属项目」，admin 也不例外），项目不存在 → 404，非管理者仍受作用域约束 → 403
- 后端 import：强制 projectId 必填（缺失 → 400「导入台账必须关联项目，请先选择目标项目」），越权/不存在同上
- 无项目归属（project_id 为空）仅为历史遗留数据，列表/导出仍按 v1.8 口径仅 admin 可见

### 18.2 自动化测试

```
命令: npx tsc --noEmit          → ✅ 0 错误
命令: npx vitest run            → ✅ 124/124 passed（与 v1.8.2 基线持平，纯函数与既有用例不受影响）
```

### 18.3 真实冒烟（临时目录独立 DB，端口 3999）

| 场景 | 预期 | 实际 |
|------|------|------|
| 新建不传 projectId | 400 | ✅ 400「台账必须关联项目，请选择所属项目」 |
| 新建空串 projectId（admin） | 400 | ✅ 400 同上 |
| 新建带合法 projectId | 201 且归属正确 | ✅ 201，log.projectId=目标项目 |
| 导入不传 projectId（合法 xlsx） | 400 | ✅ 400「导入台账必须关联项目，请先选择目标项目」 |
| 导入带 projectId | 成功 | ✅ imported=1、fieldWarnings=0 |
| 列表核对 | 记录挂项目名下 | ✅ 项目台账列表含新建记录 |

**可发布状态**: 🟢 就绪

---

## 十九、模块结合集成测试（2026-09-22）

**测试方式**: 临时目录独立 DB 启动真实 HTTP 服务（端口 3998），端到端脚本按业务链路串行调用 53 项检查，覆盖跨模块联动与 v1.8.x 全部硬校验；测试结束删除临时目录，测试数据零残留。

### 19.1 覆盖链路（53/53 通过）

| 模块链路 | 验证点 |
|----------|--------|
| 认证 | 登录/错密码拦截/注册（默认 member）/重复注册 409/auth/me |
| 项目与作用域 | 双项目创建（运维/开发实施）、未参与用户列表隔离（v1.8）、成员添加 |
| 任务联动 | 状态流转、子任务全部完成→任务自动 done、评论→通知被指派人(assign)、已读 |
| 工时 | 任务登记工时→我的工时→报表接口 |
| 预算/支出 | 创建待审批→非 finance 403→finance 通过→支出关联预算→expense/budget 汇总 |
| 台账校验 | 必联项目 400(v1.8.3)、已完成缺经验字段 400(v1.8.2)、PATCH 防清空 400、任务转台账(来源任务扩展字段) |
| 台账作用域 | 成员可见本项目、未授权项目空集防枚举、越权读 403/404、未参与用户全量空 |
| 台账导入 | 预检(重复1/新2/缺字段1)、不传项目 400、跳过式 imported=2、照导 imported=3、统计 |
| 台账导出 | xlsx 下载并读回校验行数 |
| 回收站/搜索 | 任务软删→回收站→恢复、全局搜索命中、health |

### 19.2 发现并修复的缺陷

- **🔴 新装库工时登记失败**：`task_hours` 建表语句缺 `billed_hours` 列——存量库靠启动迁移补列正常，全新初始化库直接报 `no column named billed_hours`。修复 [db.ts](file:///Volumes/DATA/Project/api/db.ts) 建表语句补列；冒烟重放验证工时登记 201
- 说明：预检/照导等其余 5 个初测失败均为测试脚本自身用例问题（非 finance 误用 admin 验证 403、越权用例选错记录、导入列名误写「作业日期」（正确列名为「日期」）、照导漏传 skipDuplicates=false），已修正用例后复测通过，产品逻辑无缺陷

### 19.3 回归

```
命令: npx tsc --noEmit          → ✅ 0 错误
命令: npx vitest run            → ✅ 124/124 passed
```

**可发布状态**: 🟢 就绪

### 19.4 后续优化（同日）：导入未识别列提醒（v1.8.4）

依测试总结报告建议②实施——「列头自动识别对陌生列名静默归入扩展列」易掩盖标准列名拼错：

- 后端：导入解析收集未识别列名，预检/正式响应均返回 `unknownColumns`（最多 10 个）；模板"自定义列示例"列与序号列豁免
- 前端：预检弹窗琥珀色提醒「已按扩展字段处理，若为标准列名拼写有误请修正」；无重复直导路径的完成通知同步提醒
- 验证：`tsc` 0 错误、vitest 124/124；临时库冒烟 2/2（误写「作业日期」正确提示 / 标准列+模板列不误报），冒烟临时数据已清除
- 文档：操作手册 25.4、概要设计书 12.1、FEATURES、测试总结报告建议②已标记实施

**可发布状态**: 🟢 就绪

---

## 二十、项目类型测试（2026-09-22，v1.8.5）

**测试范围**: 新建项目必选项目类型（运维/开发/咨询/实施）、后端强校验、类型徽章、台账入口按类型动态命名

### 20.1 单元测试（新增 5 项，全量 129/129 通过）

| # | 用例 | 结果 |
| --- | --- | --- |
| 1 | 四个合法类型均通过归一化 | ✅ |
| 2 | 首尾空白自动去除（trim 后命中白名单） | ✅ |
| 3 | 非法类型返回 null | ✅ |
| 4 | 空串/非字符串返回 null | ✅ |
| 5 | PROJECT_TYPES 常量清单恰好四类 | ✅ |

### 20.2 真实服务冒烟（5/5 通过，临时库 + 真实 HTTP 服务）

| # | 场景 | 期望 | 结果 |
| --- | --- | --- | --- |
| 1 | 创建项目不传 projectType | 400「请选择项目类型」 | ✅ |
| 2 | 传入非法类型「测试项目」 | 400「项目类型必须是：运维项目/开发项目/咨询项目/实施项目」 | ✅ |
| 3 | 合法类型创建（运维项目） | 201，回读 projectType 正确 | ✅ |
| 4 | 编辑项目修改类型 | 200，回读更新 | ✅ |
| 5 | seed 项目类型回读 | 四项目类型正确落库 | ✅ |

### 20.3 回归

- `tsc` 编译 0 错误
- vitest 全量 129/129（基线 124 → 129，新增 project-type 5 项）
- 冒烟临时环境已清理，确认无残留

**可发布状态**: 🟢 就绪

---

## 二十一、模板分类测试（2026-09-22，v1.8.6）

**测试范围**: 项目模板按五分类（运维/开发/咨询/实施 + 产品迭代）管理、创建项目选模板自动预填项目类型、存为模板自动带分类

### 21.1 单元测试（新增 7 项，全量 136/136 通过）

| # | 用例 | 结果 |
| --- | --- | --- |
| 1 | 五个合法分类均通过归一化 | ✅ |
| 2 | 首尾空白自动去除 | ✅ |
| 3 | 非法分类返回 null | ✅ |
| 4 | 空串/非字符串返回 null | ✅ |
| 5 | 常量清单恰好四类项目类型 + 产品迭代 | ✅ |
| 6 | 分类→默认类型映射（四类同名、产品迭代→开发项目） | ✅ |
| 7 | 映射后的项目类型均命中 PROJECT_TYPES 白名单 | ✅ |

### 21.2 真实服务冒烟（6/6 通过，临时库 + 真实 HTTP 服务）

| # | 场景 | 期望 | 结果 |
| --- | --- | --- | --- |
| 1 | seed 系统模板分类回读 | 网站开发=开发项目、产品迭代=产品迭代、客户支持=运维项目 | ✅ |
| 2 | 创建模板带分类「运维项目」 | 201，回读 category 正确 | ✅ |
| 3 | 创建模板非法分类「营销项目」 | 400 | ✅ |
| 4 | PATCH 修改分类 | 200，回读更新 | ✅ |
| 5 | 创建模板不传分类 | 201，category 为 null（未分类） | ✅ |
| 6 | 用系统模板创建项目（templateId 链路） | 201，projectType 正确落库 | ✅ |

### 21.3 回归

- `tsc` 编译 0 错误（修复 templates.ts update 路由 spread 类型收敛 1 处）
- vitest 全量 136/136（基线 129 → 136，新增 template-category 7 项）
- 冒烟临时环境已清理，确认无残留

**可发布状态**: 🟢 就绪

---

## 二十二、项目类型五类扩展与存量回填（2026-09-22，v1.8.7）

**变更内容**: PROJECT_TYPES 新增「产品迭代」并升格为正式类型；按用户口述回填存量有效项目类型

### 22.1 存量数据回填（按创建时间倒序 = UI 显示顺序）

| 项目 | 类型 |
| --- | --- |
| Fortune运维 | 运维项目 |
| Fortune2期 | 运维项目 |
| CFDA首营审核管理系统 | 运维项目 |
| 预实BAS系统 | 运维项目 |
| IPO供需管理系统 | 运维项目 |
| 数据可视化平台 | 开发项目 |
| 项目管理系统 | 产品迭代 |
| Fortune 后台重构 | 开发项目 |

- 8 个有效项目（deleted_at IS NULL）全部回填；13 条软删除/测试遗留项目保持未设置（不显示徽章）
- 回填后 SQL 回读核对一致

### 22.2 测试口径更新

- project-type.test.ts：四类断言 → 五类断言（含产品迭代归一化）
- template-category.test.ts：TEMPLATE_CATEGORY_DEFAULT_TYPE['产品迭代'] 断言由「开发项目」改为「产品迭代」
- `tsc` 0 错误，vitest 全量 136/136

**可发布状态**: 🟢 就绪

---

## 二十三、咨询服务模板（2026-09-22，v1.8.8）

**变更内容**: 系统模板新增第 4 个「咨询服务模板」（咨询项目类），存量库启动自动补插

### 23.1 验证结果

| 场景 | 期望 | 结果 |
| --- | --- | --- |
| 存量库（已有 3 个系统模板）启动 | 按名称幂等补插咨询模板，count 守卫不冲突 | ✅ 4 个系统模板分类齐全 |
| 全新空库启动 | initSystemTemplates 先插 3 个 + initConsultingTemplate 补插第 4 个 | ✅ 日志两条齐全，4 模板完整 |
| 咨询模板子资源 | 6 任务 / 2 预算（人力成本+外部专家）/ 4 看板列 | ✅ |

### 23.2 过程缺陷修复

- 初版将 initConsultingTemplate 调用置于 initSystemTemplates 之前：新空库会先插咨询模板导致整体 count 守卫误跳过其余 3 个系统模板——已修正调用顺序并重放验证

- `tsc` 0 错误，vitest 全量 136/136

**可发布状态**: 🟢 就绪

---

## 二十四、实施交付模板（2026-09-22，v1.8.9）

**变更内容**: 系统模板新增第 5 个「实施交付模板」（实施项目类），机制同 v1.8.8（名称幂等补插）

### 24.1 验证结果

| 场景 | 期望 | 结果 |
| --- | --- | --- |
| 存量库启动 | 补插实施交付模板 | ✅ 5 个系统模板分类齐全 |
| 全新空库启动 | initSystemTemplates + 咨询/实施两个补插均执行 | ✅ 日志三条齐全，5 模板完整 |
| 实施模板子资源 | 7 任务 / 2 预算（人力成本+硬件设备）/ 4 看板列 | ✅ |

- `tsc` 0 错误，vitest 全量 136/136，临时验证环境已清理

**可发布状态**: 🟢 就绪

---

## 二十五、运维增强与结项合并（2026-09-22，v1.9.0）

**变更内容**: 项目类型扩为六类（新增「运维增强」）+ 结项一键合并到运维项目

### 25.1 验证结果（vitest 139/139 + 真实服务冒烟 7/7）

| # | 场景 | 期望 | 结果 |
| --- | --- | --- | --- |
| 1 | 创建目标运维项目 | 201 | ✅ |
| 2 | 创建运维增强项目 | 201 | ✅ |
| 3 | 给运维增强项目记 2 条台账 | 201 ×2 | ✅ |
| 4 | 非「运维增强」项目发起合并 | 400「仅『运维增强』类型的项目支持合并到运维项目」 | ✅ |
| 5 | 执行合并 | movedOpLogs=2，targetName 正确，mergedIntoProjectId=目标 id | ✅ |
| 6 | 转绑核对 | 源项目台账 0 条 / 目标运维项目台账 2 条 | ✅ |
| 7 | 重复合并 | 400 | ✅ |

- `tsc` 0 错误，vitest 全量 **139/139**（基线 136 → 139：project-type 六类断言 + projectLedgerLabel 3 项），临时验证环境已清理

**可发布状态**: 🟢 就绪

---

## 二十六、技术债清零与 e2e/CI 沉淀（2026-09-23，v1.9.1）

**变更内容**: 一次性实施测试报告遗留的四项后续建议——ESLint 全量整改、recharts 独立 chunk、台账权限口径统一、e2e 集成测试沉淀进 CI

### 26.1 ESLint 警告全量清零（35 → 0）

| 类别 | 数量 | 处理方式 |
| --- | --- | --- |
| `@typescript-eslint/no-explicit-any` | 9 | 台账 repo/路由收窄为 `OpLog` 类型；测试文件随类型收窄去除标注 |
| `react-hooks/exhaustive-deps` | 18 | 派生数组 `|| []` 改 `useMemo` 固定引用（OpLogsPanel/Dashboard/ProjectDetail）；`handleSelectTask`/`loadColumns` 补 useCallback 依赖；GanttChart 去除多余依赖 |
| 未使用变量/导入 | 5 | 删除（TaskDrawer 的 Pencil/CSS、HoursReport 的 cumPlan、测试多余导入） |
| `no-useless-escape` | 2 | 日期正则 `\/` 转义去除 |
| `react-refresh/only-export-components` | 1 | ErrorFallback 拆分至独立组件文件；顺带删除未被引用的 `withErrorBoundary` |

### 26.2 recharts 独立 vendor chunk

- `vite.config.ts` 新增 `manualChunks`：recharts / victory-vendor / d3-* / internmap → `vendor-charts`（361.75 kB / gzip 89.28 kB），与通用 `vendor`（405.87 kB）分离，不使用图表的页面不再下载图表依赖

### 26.3 台账权限口径统一（「看得到列表打不开详情」修复）

- 背景：台账列表对全员开放（侧边栏入口 + 项目详情页内嵌），但详情/AI 端点校验 `oplog.view`，而 owner/member/guest 角色未授予权限，形成割裂（见 15.3 说明 2）
- 处理：三处同源补授——① db.ts 种子角色常量（OWNER/MEMBER/GUEST_PERM_KEYS）；② repo.ts `checkImplicitPermission` 运行时隐式映射（新注册用户走此路径）；③ `ensureSystemRolePermissions` 存量库启动幂等回填
- e2e 回归：新注册 member 调台账详情 → 200（修复前 403）

### 26.4 e2e 集成测试沉淀 + CI

- `api/db.ts` 支持 `FORTUNE_DB_PATH` 环境变量覆盖库路径（默认仍为 `data/app.db`，生产行为不变）
- 新增 [tests/e2e/cross-module.e2e.test.ts](tests/e2e/cross-module.e2e.test.ts)：子进程启动真实服务（tsx + 随机端口 + 临时库 + SMTP 置空防外发），复现集成测试链路 13 项——种子管理员登录/注册角色、项目类型硬校验、加成员、任务、工时、台账登记/列表/详情/统计、全局搜索、成员统计作用域、软删→回收站→恢复
- 新增 `.github/workflows/ci.yml`：check（tsc）→ lint（0 警告）→ test（单元 + e2e）→ build 四道闸门；npm 脚本 `test:e2e` 可单独运行
- 过程发现：测试报告 8.2「首注册用户自动为管理员」假设已失效——新装库 seed 会预置演示用户与项目，e2e 改用种子管理员登录

### 26.5 回归

- `tsc` 0 错误；ESLint 0 错误 0 警告；`vite build` 成功
- vitest 全量 **152/152**（基线 139 → 152：新增 e2e 13 项），临时库/临时目录/子进程已清理，生产 `data/app.db` 未触碰（回查无 e2e 数据残留）

**可发布状态**: 🟢 就绪

---

## 十、v1.5 回归测试（2026-09-14）

**测试范围**: 数据权限隔离整改、04 工时报表整改、项目删除审批入口、趋势图基线、工时偏差口径
**测试环境**: macOS / Node.js v22 / better-sqlite3 / Vitest
**总体结论**: ✅ 全部通过

### 10.1 自动化测试

```
命令: npx tsc --noEmit          → ✅ 0 errors
命令: npx vitest run            → ✅ 48/48 passed (tests/stats-scope.test.ts)
命令: npx vite build            → ✅ 构建成功, HoursReport 独立分块正常
```

测试覆盖（48 项断言）：scopedProjectIds 三态语义（admin→不限制 / 普通用户→参与项目集合 / 无项目→[]）、resolveScope 越权→[]、countByStatus([]) 全 0、trendDaily([])→[]、burndown([]) 空集、工时任务计划汇总含人员过滤与未指派归集、软删除任务排除等。

### 10.2 数据权限隔离（tester 反馈回归）

| 场景 | 预期 | 实际 |
|------|------|------|
| 新注册用户（未参与任何项目）查仪表板/统计 | 任务总量 0、项目数 0 | ✅ 全 0（修复前显示全局 48/50） |
| 普通用户指定越权 projectId | 返回空集 | ✅ 空集，不报错、不泄露 |
| 端到端：注册→建项目→建任务→查统计 | 仅返回自己项目数据 | ✅ 1/1 |

### 10.3 工时报表（04 模块，tester 反馈回归）

| 场景 | 预期 | 实际 |
|------|------|------|
| 项目含进行中 48h + 待办 24h 任务，未登记工时 | 任务计划工时 72，登记明细空 | ✅ 72/0（修复前显示 0 工时） |
| 登记工时后 | 明细含项目名/任务名/登记人/计划/内部/计费 | ✅ 正确 |
| 日期筛选 | 服务端过滤生效 | ✅ |
| 人员筛选（含未指派任务） | 显示未指派提示 | ✅ |
| 越权用户查他人项目 | 全空 | ✅ |

### 10.4 工时趋势图显示逻辑

| 规则 | 测试数据 | 结果 |
|------|----------|------|
| 正常区间按天均摊 | 40h / 5 天 | ✅ 8.0/天 |
| 多任务基线叠加 | 单日任务 10h 与正常任务重叠 | ✅ 当天 18 |
| 无日期任务 | 20h 无起止 | ✅ 不生成，计入 skipped |
| 超长区间（>60 天） | 12h / 365 天 | ✅ 不生成基线（修复了原封顶 90 天拉爆时间轴的缺陷），skipped=32 精确 |
| 登记数据合并 | 蓝柱/绿柱/黄线同轴对齐 | ✅ |

### 10.5 工时偏差计算逻辑

公式：偏差 = 内部实际合计 − 任务计划合计；偏差率 = 偏差 ÷ 任务计划（分母 0 → 0%）

| 场景 | 计算 | 结果 |
|------|------|------|
| 全部人员 | 45 − 50 = −5（−10%） | ✅ 绿色 |
| 筛选甲（按 assignee 归集计划） | 23 − 40 = −17（−42.5%） | ✅ |
| 筛选乙（超支） | 22 − 10 = +12（+120%） | ✅ 红色 |
| 日期筛选只影响实际侧 | 5 − 50 = −45 | ✅ |
| 无登记日期 | 0 − 50 = −50 | ✅ |
| 空项目除零 | 0 / 0 → 0% | ✅ 不 NaN |

### 10.6 项目删除审批

| 场景 | 结果 |
|------|------|
| 普通用户查待审批列表 → 403 | ✅ |
| admin 查列表/驳回(附原因)/批准 | ✅ |
| 处理后队列清零 | ✅ |
| 驳回项目恢复原状（deletedAt 清除） | ✅ |
| 批准项目进入回收站 | ✅ |
| 批准+驳回双通知送达申请人 | ✅ |

### 10.7 本轮附带修复

- `/stats/project-cost` SQL 笛卡尔积放大修复（预算 1000 曾被算成 2000），并去 N+1 改批量查询
- 登记工时表单补「计划工时」字段（原表单缺失导致登记计划恒 0）
- 删除死代码 `listMyHours`（前端调用）

**可发布状态**: 🟢 就绪

---

## 十一、团队页权限分级与附带修复测试（2026-09-17）

**测试范围**: 团队页 team.view 权限分级（接口分级返回 + 侧边栏入口过滤 + 直访空态）、甘特图日期闭区间修复、工时报表默认值/联动
**测试环境**: macOS / Node.js v22 / better-sqlite3 / Vitest / 浏览器双身份实测

### 11.1 自动化测试

- `npx tsc --noEmit` → ✅ 0 错误
- `npx vitest run` → ✅ 48/48 passed

### 11.2 测试中发现并修复的问题

| # | 问题 | 修复 |
|---|------|------|
| 1 | member 侧边栏仍显示「团队」入口（只锁了数据没锁入口） | AppLayout 按权限过滤 navItems，无 team.view 者隐藏入口 |
| 2 | member 直访 `/team` 显示一批无邮箱空卡片 | Team 页识别 `limited` 响应，显示空态提示「仅管理员、财务与项目负责人可见」 |

### 11.3 API 权限矩阵

| 场景 | 期望 | 结果 |
|------|------|------|
| admin / finance / owner 调 `GET /api/team` | 完整通讯录（全部成员含邮箱 + 任务统计） | ✅ |
| member / guest 调 `GET /api/team` | 协作最小字段（0 邮箱 / 0 统计）+ `limited: true` | ✅ |
| 未登录调 `GET /api/team` | 401 | ✅ |
| member 新建用户（写操作） | 403 | ✅ |

### 11.4 浏览器实测（member / admin 双身份）

| 场景 | 结果 |
|------|------|
| member 登录后侧边栏无「团队」入口 | ✅ |
| member 直访 `/team` 显示空态提示（无空卡片） | ✅ |
| member 任务创建负责人下拉正常（24 项可选） | ✅ |
| 工时报表同名人员序号消歧（如「成员乙（1）」） | ✅ |
| admin 登录后团队页完整渲染（邮箱+统计） | ✅ |

### 11.5 附带修复验证

| 场景 | 结果 |
|------|------|
| 甘特图日期闭区间：同一天起止占 1 格，9/1~9/3 覆盖 9/1、9/2、9/3 三天 | ✅ |
| 工时报表期间默认当月 1 日~最后一天、项目默认当前用户负责的第一个项目 | ✅ |
| 选中具体项目时人员下拉仅列该项目成员 + 历史参与人 | ✅ |

### 11.6 探针数据清理

测试所用探针账号（member/finance/guest 共 4 个）及项目成员关系已全部删除，用户数恢复至 22。

**可发布状态**: 🟢 就绪

---

## 十二、运维台账 AI 与体验改造测试（2026-09-21）

**测试范围**: 台账体验小改造（相似案例点击跳转、问题/方案复制、只读详情视图）、AI 流式输出（P1b）、召回升级（P1a：LIKE 全文扩展 + 可选 embedding 语义重排 + 向量缓存表）
**测试环境**: macOS / Node.js v22 / better-sqlite3 / Vitest / 内部 vLLM(Qwen3.6-35B-A3B-AWQ) 实测

### 12.1 自动化测试

```
命令: npx tsc --noEmit          → ✅ 0 错误
命令: npx vitest run            → ✅ 93/93 passed (9 个测试文件,基线 81 → 93)
```

本轮新增/调整的测试（12 项）：

| 测试文件 | 数量 | 覆盖点 |
|---|---|---|
| tests/llm-embed.test.ts | 4 | 未配置抛 503、embeddings 按 index 还原顺序、BASE_URL/KEY 回落 chat 配置、返回不完整抛 502 |
| tests/recall.test.ts（追加） | 8 | 余弦相似度（同向/正交/反向/零向量）、融合重排排序与 topK 截断、向量缓存读写/换模型失效/update 与软删除钩子/损坏行忽略 |

存量回归（81 项保持通过）：problem-expand 8（展开交互纯逻辑）、oplogs-expand.dom 5（组件级展开/收起/独立状态/空方案占位）、llm-stream 4（SSE 行解析/跨 chunk 断行/空流 502/未配置 503）、recall 关键词与双路召回、stats-scope 18、subtask-status 17、auth-token 13 等。

### 12.2 LLM 实测（内部服务）

| 场景 | 结果 |
|------|------|
| vLLM `/v1/models` 连通与 `/v1/chat/completions` JSON 输出（response_format: json_object） | ✅ 返回纯 JSON |
| AI 分析 JSON 路径（summary/possibleCauses/suggestedSteps/risks） | ✅ |
| 本地服务 API_KEY 非空占位校验（enabled() 三变量） | ✅ |

### 12.3 降级链路验证

- 流式端点异常 → 前端自动降级调用非流式 `analyze`（一次）✅
- embedding 失败/未配置 → 召回静默退回关键词排序，分析不受影响 ✅
- 零召回案例 → 直接返回降级结论，不消耗模型调用 ✅

**可发布状态**: 🟢 就绪

## 十三、AI 排障助手（对话式排障 P2）测试（2026-09-21）

**测试范围**: chat-stream SSE 多轮流式端点、buildChatMessages Prompt 组装、AiChatDrawer 组件交互、SSE 断开检测修复、chatStream 空闲超时
**测试环境**: macOS / Node.js v22 / Vitest / 内部 vLLM(Qwen3.6-35B-A3B-AWQ) 实测

### 13.1 自动化测试

```
命令: npx tsc --noEmit          → ✅ 0 错误
命令: npx vitest run            → ✅ 99/99 passed (11 个测试文件,基线 93 → 99)
```

本轮新增的测试（6 项）：

| 测试文件 | 数量 | 覆盖点 |
|---|---|---|
| tests/prompts-chat.test.ts | 3 | system 规则与案例注入、历史轮次按序透传、空召回「无台账依据」提示、字段截断与空值兜底 |
| tests/ai-chat-drawer.dom.test.tsx | 3 | 空态提示语点击即发送（请求含历史+projectId）、流式增量拼接与案例卡片点击溯源、发送失败通知并保留部分回答、停止生成保留已生成部分 |

### 13.2 真实冒烟（chat-stream 端到端）

| 场景 | 结果 |
|------|------|
| 登录 → POST /api/op-logs/ai/chat-stream（零案例问题） | ✅ HTTP 200，SSE 事件 meta(0 案例)→delta(301 个)→done，2s 完成 |
| 无召回时的行为约束 | ✅ 回答先声明「台账中未检索到相似案例」再给通用建议（system 约束生效） |
| 长回答流式（原固定 60s 总时长超时场景） | ✅ 空闲超时修复后正常完成 |

### 13.3 本轮发现并修复的缺陷

1. **SSE 客户端断开检测误杀**（严重）:`req.on('close')` 在请求体读取完毕后即触发，导致 upstream LLM 请求被立即中止、流式必然超时。修复为监听 `res.on('close')` 并以 `res.writableEnded` 排除正常结束；`analyze-stream` 同源隐患一并修复
2. **chatStream 固定总时长超时**:60s 定时器覆盖整个流生命周期，长回答/思维链模型必超时。改为空闲超时（每收到数据重置计时），仅流停滞超时才中止

**可发布状态**: 🟢 就绪
